# Repo Memory

Seeded from the current GitHub-style repo memory context for this repository.

Format per entry:

- Fact: the durable repo fact to inject
- Why it matters: the short reason an agent should care
- Citations: the best current grounding for that fact

## Architecture and ownership

### Live Codex extension path

- Fact: The only live registered Codex extension path is `extensions/codex_ext`; `extensions/codex` was removed as dead code and tooling/package data now target `codex_ext`.
- Why it matters: this avoids editing the wrong extension tree and keeps schema/runtime work aimed at the path that the loader actually instantiates.
- Citations: `extensions/extensions.json:3-18`; `pyproject.toml:38-41`; `scripts/codex_schema_extract.py:5-6,86,155`

### Codex auth surface today

- Fact: Codex protocol/runtime include account auth schemas, but current `codex` and `codex-ext` clients only use model/thread/turn RPCs and do not call account methods yet.
- Why it matters: auth-related work still needs explicit client wiring; the schemas existing upstream does not mean the local extension already uses them.
- Citations: `extensions/codex_ext/protocol.py:172-179,281-288,313`; `extensions/codex_ext/runtime_protocol.py:594-675,819-842,1314-1320`; `extensions/codex_ext/client.py:303-305,328,437-543`

### Session picker binding

- Fact: Extension `session_picker` fields are rendered by `static/modals/settings_schema.js`, and `server.py` binds `settings.session` as a one-time thread/session selection.
- Why it matters: session binding behavior is shared and schema-driven, so new session-picker logic should plug into the existing modal/server path instead of inventing extension-specific selectors.
- Citations: `agent_log_server/static/modals/settings_schema.js:298-327,512-514`; `extensions/copilot_sdk/settings_schema.json:15-20`; `extensions/codex_ext/runtime_protocol.py:1194-1201`; `agent_log_server/server.py:5977-6005`

### Rollout picker ownership

- Fact: The rollout picker is a shared frontend resource for extensions; the generic frontend now exposes a provider hook for it, while Codex rollout transcript import lives inside each Codex extension package instead of `server.py`.
- Why it matters: rollout/session-import behavior must remain extension-owned, but the UI shell can stay generic. Future work should register picker providers from extensions rather than reintroducing server-owned `/api/appserver/rollouts*` routes or hardcoded generic Socket.IO rollout calls.
- Citations: `User input: the rollout picker is a shared frontend resource for extensions`; `agent_log_server/static/js/codex_agent/boot/init_flow.js:132-149`; `agent_log_server/static/js/codex_agent/settings/ui_flow.js:357-425`; `extensions/codex_ext/rollout_import.py:1-179`; `extensions/codex_ext_exp/rollout_import.py:1-179`

### Extension settings independence

- Fact: Codex extension settings must be fully schema-driven and must not depend on hidden builtin-Codex modal inputs for save or hydrate.
- Why it matters: the builtin Codex settings block is intentionally not the extension settings path. Future modal/save work must treat extension schema fields as the source of truth so extension settings remain independent and old builtin state does not overwrite extension-managed values.
- Citations: `User input: Yeah we recently changed the settings modal so that the built in codex extension portions are removed entirely. I've been asking over and over again to make sure that the extensions are completely independent from the built-in route. Both extensions. The default server routes need to be served and the extension settings need to be 100% self supported.`; `agent_log_server/static/js/codex_agent/settings/save_flow.js:53-165`; `agent_log_server/static/modals/settings_schema.js:230-247,358-360`; `extensions/codex_ext/runtime_protocol.py:1306-1312`; `extensions/codex_ext_exp/runtime_protocol.py:1306-1312`

### Extension availability

- Fact: Extension availability is driven by `enabled && dependency_ok`; inactive extensions stay out of agent dropdowns while splash settings show `dependency_message`.
- Why it matters: readiness and UX gating already have a single contract, so new dependency/auth checks should feed that pipeline instead of creating a second availability system.
- Citations: `extensions/__init__.py:368-445`; `agent_log_server/static/ui/extension_settings.js:21-30,101-145`; `agent_log_server/static/js/codex_agent/settings/ui_flow.js:383-394`

### Legacy codex removal
- Fact: Legacy builtin Codex runtime/fallback has been removed from `agent_log_server/server.py`. Old `/api/appserver/*` Codex-only endpoints now either route through extensions or return explicit `legacy_disabled` no-op results via `_legacy_builtin_codex_disabled_result()`, and the builtin shell/RPC helper chain is gone.
- Why it matters: `codex-ext` and `codex-ext-exp` are now the only supported Codex runtimes. Future work should not reintroduce builtin shell/RPC fallback or revive `_get_or_start_appserver_shell()`-style helpers; any remaining legacy callers should move to the generic extension path instead.
- Citations: `agent_log_server/server.py:1235-1248`; `agent_log_server/server.py:6172-6268`; `agent_log_server/server.py:6414-6694`; `agent_log_server/server.py:7149-7155`
### codex-ext-exp shellspec isolation

- Fact: `codex-ext-exp` has its own shellspec at `extensions/codex_ext_exp/shellspec/app_server_exp.yaml` pointing at the patched binary (`~/downloads/codex/codex-rs/target/debug/codex-app-server`). Transport label is `"app-server:codex-experimental"`, spec IDs are `app_server_exp` and `app_server_exp_observed`. This is fully isolated from `codex-ext` (which uses `"app-server:codex-extension"` and `app_server_observed`).
- Why it matters: `codex-ext-exp` runs a separate app-server process from `codex-ext`. There is no process adoption between them. The patched binary supports `developerInstructions` in `TurnStartParams` for mid-thread developer instruction updates.
- Citations: `extensions/codex_ext_exp/shellspec/app_server_exp.yaml`; `extensions/codex_ext_exp/transport.py:20,382,388`; `extensions/codex_ext_exp/manifest.json:10-12`; `worktrees/notes/PATCHED_APP_SERVER.md`

### Copilot CLI process CWD isolation

- Fact: CopilotClient singleton in `extensions/copilot_sdk/client.py` now explicitly sets `cwd` to `$HOME` to prevent the CLI process from inheriting the framework-shell launch CWD (typically TE2/mrselect6). Without this, the CLI reads AGENTS.md from the inherited CWD and injects the wrong repo's instructions into every conversation. Per-session `workingDirectory` handles repo-specific context.
- Why it matters: The agent_log_server is launched as a TE2 framework shell, inheriting TE2's CWD. The Copilot CLI reads AGENTS.md from its process CWD at startup and bakes it into the system prompt foundation. This caused TE2-internal instructions (WBA, etc.) to leak into conversations in unrelated repos.
- Citations: `extensions/copilot_sdk/client.py:1791`; `extensions/copilot_sdk/_vendor/copilot/client.py:186,1291`

### Codex runtime ownership boundary
- Fact: Codex extension runtime logic must be fully self-contained inside the extension directories. Outside `extensions/`, only generic hook/API surfaces may exist; any Codex-specific runtime handling left in `agent_log_server/server.py` is a legacy removal target, not behavior to preserve.
- Why it matters: During `server.py` decomposition, Codex-specific runtime/item/tool routing must not be treated as a reusable fallback slice. If a server-side seam only exists for Codex protocol behavior and is not a truly generic extension hook, it should be removed or moved behind extension-owned code rather than kept in the main app.
- Citations: `User input: THE CODEX EXTENSION SHOULD HAVE ***NO GOD DAMN MOTHERFUCKING SERVER.PY FALLBACKS AND SHOULD BE FUCKING SELF SUPPORTED.... PERIOD***`; `User input: ***NO, ABSOLUTELY NONE, EVER, IN A FUCKING MILLION YEARS SHOULD EXTENSION LOGIC EXIST OUTSIDE OF THE EXTENSION DIRECTORY UNLESS IT'S THE GENERIC HOOK/API SURFACE THAT THEY USE TO TALK TO THE MAIN APP*** PERIOD.`
### Legacy raw appserver router removal
- Fact: The legacy raw Codex app-server event router has been removed from `agent_log_server/server.py`. Event translation now belongs in extension routers behind the generic `ext_loader.route_event(...)` hook instead of a server-owned item/tool translator block.
- Why it matters: Future `server.py` decomposition should not preserve or re-extract a Codex-specific raw event-routing seam into the main app. If raw backend events need translation, that logic belongs inside the extension directories unless the behavior is truly generic across extensions.
- Citations: `extensions/__init__.py:2460-2481`; `User input: THE CODEX EXTENSION SHOULD HAVE ***NO GOD DAMN MOTHERFUCKING SERVER.PY FALLBACKS AND SHOULD BE FUCKING SELF SUPPORTED.... PERIOD***`; `User input: ***NO, ABSOLUTELY NONE, EVER, IN A FUCKING MILLION YEARS SHOULD EXTENSION LOGIC EXIST OUTSIDE OF THE EXTENSION DIRECTORY UNLESS IT'S THE GENERIC HOOK/API SURFACE THAT THEY USE TO TALK TO THE MAIN APP*** PERIOD.`
### Page/bootstrap split
- Fact: The FastAPI/fastcore page stack is now split three ways: `agent_log_server/index.py` owns app/socketio construction, `agent_log_server/page_routes.py` registers the page routes, and `agent_log_server/web_pages.py` owns the large `fastcore.xml` render helpers.
- Why it matters: future webpage/bootstrap decomposition should keep page rendering out of `server.py` and avoid stuffing business logic into `index.py`. New page/UI changes should target `web_pages.py` or `page_routes.py`, while bootstrap wiring changes should target `index.py`.
- Citations: `agent_log_server/index.py:9-27`; `agent_log_server/page_routes.py:19-63`; `agent_log_server/web_pages.py:31-541`; `agent_log_server/server.py:49-50,144-147,4824`
### Appserver HTTP split
- Fact: Conversation/appserver HTTP routes now live in `agent_log_server/appserver_routes.py`, while `server.py` instantiates `AppserverRoutes` and registers them with `register_appserver_routes(...)`.
- Why it matters: future appserver HTTP work should target `appserver_routes.py` instead of rebuilding handlers in `server.py`. This keeps the remaining `server.py` decomposition focused on PTY/WebSocket and preserves the extracted route-registration pattern already established by the page and host splits.
- Citations: `agent_log_server/appserver_routes.py:37-55`; `agent_log_server/appserver_routes.py:1546-1604`; `agent_log_server/server.py:2945-3070,3831`

### Conversation store split
- Fact: App-server config persistence, conversation index/pin normalization, conversation path helpers, and conversation meta load/save now live in `agent_log_server/conversation_store.py`; `server.py` imports them as aliases and only wires the extension-config normalizer and pending-context refresh hook.
- Why it matters: future conversation metadata/config work should target `conversation_store.py` first instead of rebuilding file-path and JSON persistence logic inside `server.py`. This keeps `server.py` closer to orchestration while preserving the existing generic route dependency wiring.
- Citations: `agent_log_server/conversation_store.py:23-118`; `agent_log_server/conversation_store.py:125-253`; `agent_log_server/server.py:390-428`; `agent_log_server/server.py:2252-2275`

### TE2 sync split
- Fact: Cached TE2 asset sync and Codex TE2 MCP config sync now live in `agent_log_server/te2_sync.py`; `server.py` instantiates `Te2SyncHelpers` and aliases its sync/write methods into the existing startup and route wiring.
- Why it matters: future TE2 cache/config synchronization changes should target `te2_sync.py` rather than adding more file-copy and TOML-edit logic back into `server.py`. This preserves the decomposition direction where `server.py` owns lifecycle orchestration while standalone helper modules own concrete persistence/sync work.
- Citations: `agent_log_server/te2_sync.py:17-118`; `agent_log_server/server.py:417-428`

### Loader registry metadata ownership
- Fact: Loader-built extension registry entries already own the authoritative install/runtime metadata for each extension, including `path`, `source_root`, `install_source`, `installer_meta`, `version`, and `schema_version`; HTTP/admin callers read that metadata back through `ext_loader.list_extensions()`, `get_extension_info()`, and `get_extension_asset_path()`.
- Why it matters: any remaining "loader metadata refactor" work should treat the registry as the existing source of truth rather than recreating repo-local path assumptions in `server.py`, handlers, or route helpers. Future extension install/admin work should extend loader state centrally instead of adding side-band metadata plumbing.
- Citations: `extensions/__init__.py:588-613`; `agent_log_server/extension_api.py:80,109,139,402,620,758`

### Agent-log subsystem split
- Fact: The agent-log/message subsystem now lives in `agent_log_server/agent_log_subsystem.py`; `server.py` instantiates `AgentLogSubsystem`, registers its `/api/messages*` and `/ws` routes, and passes `_agent_log.append_record` / `_agent_log.read_records` into the appserver Socket.IO dependency container.
- Why it matters: future agent-log work should target the extracted subsystem instead of rebuilding message-log routes or websocket broadcast helpers inside `server.py`. This keeps `server.py` closer to orchestration-only wiring and makes the log surface reusable from both HTTP and Socket.IO without another inline helper wall.
- Citations: `agent_log_server/agent_log_subsystem.py:18-185`; `agent_log_server/server.py:134`; `agent_log_server/server.py:1884-1887`; `agent_log_server/server.py:1911-1976`; `agent_log_server/server.py:2003`

### Meta-envelope cleanup
- Fact: The old PTY-era `pending_cmd_buffer` / meta-envelope injection cluster has been removed from `server.py`; the only remaining generic envelope logic is transcript stripping in `agent_log_server/transcript_sanitizer.py`, while live repo-memory developer-instruction updates remain in `pending_context.py`.
- Why it matters: future work should not treat command buffering as an active server subsystem or try to extract the dead PTY path into another module. If transcript replay needs envelope stripping, use the shared sanitizer; if repo-memory context updates need changes, work in `pending_context.py` / `codex-ext-exp`, not in `server.py`.
- Citations: `agent_log_server/transcript_sanitizer.py:1-24`; `agent_log_server/appserver_routes.py:27,881-940`; `agent_log_server/server.py:63,109,127`
## Critical contracts

### Composer draft restore

- Fact: Failed sends restore drafts server-side via `meta["draft"]` + `draft_update` only when an extension returns `restore_draft: true`; visible failure uses the shared error contract.
- Why it matters: send-failure UX is backend-owned and opt-in, which avoids duplicate user-turn artifacts and keeps draft restoration consistent across extensions.
- Citations: `agent_log_server/server.py:6565-6706,7523-7624`; `extensions/__init__.py:1320-1335`; `TRANSCRIPT_CARD_CONTRACTS.md:269-290`

### Conversation error contract

- Fact: User-visible conversation errors use live `type: "error"` and replay `role: "error"`; `rpc_error` is reserved for app-server pending-RPC failures.
- Why it matters: extensions and frontend rendering need one visible error contract for live and replay parity; `rpc_error` is not the generic user-facing lane.
- Citations: `TRANSCRIPT_CARD_CONTRACTS.md:222-248`; `extensions/copilot_sdk/router.py:1459-1496`; `agent_log_server/static/js/codex_agent/events/router.js:196-199`

### Token/footer ownership

- Fact: Footer token/context is backend-owned: routers must emit `context_window` in `token_count` / `token_usage`; frontend must not do extension-specific recovery.
- Why it matters: footer correctness depends on the backend delivering complete token metadata, so frontend patches should not try to reconstruct missing extension fields.
- Citations: `TRANSCRIPT_CARD_CONTRACTS.md:71-116`; `extensions/copilot_sdk/router.py:179-222`

### Transcript replay roles

- Fact: Legacy Codex rich replay for tool usage depends on transcript roles `mcp_tool` and `web_search`, not flattened `command` rows.
- Why it matters: replay fidelity for Codex tools still depends on those transcript role shapes, so router/storage changes must preserve them.
- Citations: `agent_log_server/server.py:4692-4816`; `agent_log_server/static/codex_agent.js:3134-3251`

### Runtime transport

- Fact: Runtime UI/backend contracts must stay Socket.IO-only; do not add HTTP fallbacks without explicit user approval.
- Why it matters: this is a repo-level architectural rule, especially for shared harness behavior and extension runtime contracts.
- Citations: `AGENTS.md:69-78`

### Socket.IO namespace patterns

- Fact: The repo already uses `python-socketio` `AsyncServer`, `AsyncClient`, `AsyncNamespace`, and multiple namespaces like `/appserver` and `/sidebar_ipc`.
- Why it matters: This is a durable architectural fact about the existing transport stack. It will help future work choose repo-native IPC designs and avoid inventing a parallel transport when namespace/client patterns already exist in this codebase.
- Citations: `agent_log_server/server.py:136-153,3635-3700`; `te2_services/appserver_transport.py:37-110,265-280`; `requirements.txt:3`

### Framework-shell secret pattern

- Fact: Both the main server and `mcp_agent_pty_server.py` derive a stable repo-root-based framework-shell secret from `~/.cache/framework_shells/runtimes/<fingerprint>/secret`.
- Why it matters: This is the framework-shell trust mechanism already present in the repo. It is useful background for process isolation work, but it is separate from the dedicated app-server IPC secret used by interactive MCP flows.
- Citations: `agent_log_server/server.py:3287-3314`; `mcp_agent_pty_server.py:28-50`

### IPC auth secret

- Fact: The private `/ipc` Socket.IO namespace uses a dedicated random secret stored at `~/.cache/app_server/ipc_secret` via `agent_log_server/ipc_auth.py`.
- Why it matters: interactive MCP flows such as `ask_user` authenticate with this local cache-backed secret instead of reusing the framework-shell secret. KB writes no longer send repo-memory delta notifications over this route.
- Citations: `agent_log_server/ipc_auth.py`; `agent_log_server/server.py`; `mcp_agent_pty_server.py`

### Targeted reload shell exit

- Fact: ALS-RS targeted extension reload now explicitly terminates matching framework-shell transport shells by the changed extension manifest's `agent.shellspec` spec id plus current app scope before module re-import/wait-ready; it does not rely only on the extension transport's cached `_shell_id`.
- Why it matters: observed app-server extensions like `gemini-app-server` can survive reload if the old transport lost its in-memory shell id even though the shell is still running. Future reload work should preserve the adapter-owned framework-shell termination backstop alongside extension `stop_client` / `shutdown_client` hooks.
- Citations: `agent_log_server_rs/adapters/extension_adapter.py`; `rust/crates/als-server/src/extension_routes.rs`; `/data/data/com.termux/files/home/mrselect6/worktrees/framework-shells/framework_shells/manager.py`

### Ask-user interaction IDs

- Fact: `agent-pty-blocks.ask_user` uses the conversation's own `conversationId` (from `CONVERSATION_ID` env var injected at MCP stdio launch) as the canonical `requestId`. No derivation, no hashing, no side-band registration. Each MCP server process is 1:1 per conversation and ask_user is synchronous per thread, so `conversationId` is collision-free. The old `canonical_ask_user_request_id()` hash helper and `_ASK_USER_REQUEST_ID_RE` regex have been removed.
- Why it matters: ask-user cards render with `requestId = conversationId`, the MCP tool registers its pending future under the same key, and the `/ipc ask_user_response` event resolves it by matching that key. Future ask-user work must use this conversationId contract and must not reinvent ID generation, side-band registration, or hash derivation.
- Citations: `mcp_agent_pty_server.py:2357-2449`; `extensions/codex_ext/router.py:1916,2494,2803`; `extensions/codex_ext_exp/router.py:1916,2494,2803`; `extensions/codex_ext/transport.py:656`; `extensions/codex_ext_exp/transport.py:656`; `extensions/codex_ext/runtime_protocol.py`; `extensions/codex_ext_exp/runtime_protocol.py`; `extensions/copilot_sdk/te2_runtime.py`

### Ask-user live bind seam

- Fact: Live Codex/Codex-exp `agent-pty-blocks.ask_user` calls bind on the `item/started` notification carrying an `item.type == "mcpToolCall"` tool item, not on an emitted `item/tool/call` server-request in the tested runtime path.
- Why it matters: ask-user card synthesis must follow the same live MCP tool-call parsing path as other MCP tool cards in the Codex routers. Assuming a live `item/tool/call` request for this path breaks binding, leaves `meta.json` empty, and causes the MCP tool to hang waiting for a response path that was never created.
- Citations: `extensions/codex_ext/transport.py:654-700`; `extensions/codex_ext/router.py:1737-1938,2411-2484`; `extensions/codex_ext_exp/transport.py:654-700`; `extensions/codex_ext_exp/router.py:1737-1938,2411-2484`; `/data/data/com.termux/files/home/.cache/framework_shells/runtimes/b4f2d44683b344a8/0a37fcbc242ec454/logs/fs_1774985801_bdf1da55.stderr.log:141-143`

### Ask-user pending/resolved ordering

- Fact: Pending `ask_user` cards now carry backend-owned unresolved ordering (`order_id = -1`) in their persisted `meta.json` render event, and the resolved handoff must merge the recorded transcript metadata (`nid`, `card_id`, `order_id`) back into the broadcast event instead of letting the frontend invent a synthetic resolved card id.
- Why it matters: `ask_user` cards are two phases of one backend-owned transcript contract: pending/live in `meta.json`, then resolved in `transcript.jsonl`. Future `ask_user` work must keep unresolved state out of the transcript order lane, let transcript append assign the real positive order on resolution, and avoid reintroducing frontend-only `ask_user_resolved_*` identity hacks.
- Citations: `agent_log_server/server.py:840-866,1567-1584,1630-1644`; `agent_log_server/ask_user_interactions.py:81-91,296-309,374-386`; `agent_log_server/static/js/codex_agent/approvals/ui.ts:724-757`; `agent_log_server/static/js/codex_agent/timeline/replay.ts:556-575`

### Copilot MCP ask_user routing

- Fact: In `copilot-sdk`, `agent-pty-blocks.ask_user` must be intercepted before generic tool rendering: the client event queue handles MCP tool start/complete events, persists a pending approval descriptor with `request_id = conversationId` and `card_id = tool_call_id`, suppresses the generic MCP tool row, and leaves response/handoff finalization to ALS-RS `/ipc` ask-user ack handling.
- Why it matters: Copilot and Codex both special-case this MCP tool as a Rust-owned ask-user approval, not a normal provider approval. If Copilot lets `agent-pty-blocks.ask_user` fall through as a normal MCP tool, no pending approval is written to `meta.json`, no ask-user card appears, and the tool times out waiting for a response path that was never created.
- Citations: `extensions/copilot_sdk/client.py`; `extensions/copilot_sdk/manifest.json`; `extensions/copilot_sdk/ui/request_cards/copilot_request_card.js`; `rust/crates/als-server/src/ipc.rs`; `rust/crates/als-server/src/conversation_rpc.rs`
### Resume/send virtual ack

- Fact: `codex-ext` thread resume treats `thread/status/changed` `idle` as virtual ack and fails fast on `mcp_startup_update` with `status.state = failed`.
- Why it matters: successful resume is event-first, not giant-response-first, so future resume fixes should preserve that transport behavior instead of waiting on the full `thread/resume` payload.
- Citations: `extensions/codex_ext/transport.py:857-923,1016-1030`; `extensions/codex_ext/client.py:371-397,1019-1023`

### TE2 relay passthrough

- Fact: The TE2 appserver relay only forwards Socket.IO events that have explicit `on_<event>` passthrough methods; missing `on_get_runtime_options` broke proxied schema dynamic selects even though the direct `:12359` portal worked.
- Why it matters: proxy-only UI failures can come from the TE2 relay shim dropping an otherwise-valid appserver event, so future debugging should compare direct portal behavior against the relay passthrough table before changing frontend logic or the reverse proxy.
- Citations: `te2_services/appserver_transport.py:163-179`; `agent_log_server/server.py:361-369,789-796`; `agent_log_server/static/modals/settings_schema.js:95-103,457-492`

### TE2 proxy rewrite

- Fact: The live TE2 `proxy_shell` can rewrite absolute `/api/...` references inside served JavaScript and inject Socket.IO path adjustments, so exact source-string checks are not proxy-safe for runtime-option detection.
- Why it matters: direct `:12359` behavior can differ from proxied `:8089` behavior because the wrapper changes the delivered asset shape, so future fixes should reason about the live proxy path and match runtime sources by path shape/suffix instead of the unproxied literal.
- Citations: `/data/data/com.termux/files/home/mrselect6/app/apps/codex_agent/manifest.json:15-33`; `agent_log_server/static/modals/settings_schema.js:67-105,481-484`; User input: It works fine from Port :12359 it only fails on 8089

### Assistant markdown render split
- Fact: Assistant markdown now uses SMD only for live token streaming; once finalized, assistant cards are rendered through the markdown-it event renderer, and static/replay assistant cards also use markdown-it.
- Why it matters: underscore-heavy file paths and markdown links remain stream-friendly during live generation but finalize to the safer renderer, preventing malformed anchors and emphasis corruption in transcript cards.
- Citations: `agent_log_server/static/js/codex_agent/markdown.js:310-338`; `agent_log_server/static/js/codex_agent/assistant_stream.js:11,78`; `agent_log_server/static/codex_agent.js:3043`; `agent_log_server/static/codex_agent.js:3658`
### Runtime options ownership
- Fact: `/api/appserver/runtime_options` is now a generic extension-owned surface: `server.py` only resolves the active conversation/agent and forwards through `ext_loader.get_runtime_options(...)`; it must not provide Codex-specific option descriptors.
- Why it matters: runtime footer/control options are part of extension ownership, so future work should add or change option enums and current-state logic inside the extension/runtime protocol path rather than reintroducing server-owned Codex-shaped responses.
- Citations: `agent_log_server/server.py:5401-5458`; `extensions/__init__.py:2367-2398`
### Toast RPC contract
- Fact: Toast live delivery is now a conversations-RPC contract: canonical notification is `conversation.toast` on `/rpc/conversations`, while legacy `/appserver` may only mirror it for compatibility. Reply-capable toast sends should reuse `conversation.send` with typed `toast_context` metadata (`toast_id`, `kind`, optional `turn_id`, optional `assistant_id`). Toast-specific backend normalization, validation, and future toast-surface helpers should live in a separate generic module under `agent_log_server/`, while `server.py` stays orchestration/fan-out only.
- Why it matters: Future toast/reply work should anchor on the typed conversations RPC surface first, keep `/appserver` as compatibility-only, preserve one shared send path for normal user messages and toast replies, and avoid pushing growing toast policy back into `server.py`.
- Citations: `agent_log_server/conversations_rpc_contract.py:30-67,141-145,436-450`; `agent_log_server/static/js/codex_agent/rpc/conversations/contract.ts:102-180`; `agent_log_server/static/js/codex_agent/rpc/conversations/client.ts:515-545`; `agent_log_server/server.py:1579-1623`; `TRANSCRIPT_CARD_CONTRACTS.md:440-511`; `worktrees/notes/turn_summary_toasts_plan.md:77-112,278-289`
### ALS-RS synced composer drafts

- Fact: ALS-RS broadcasts `conversation.draft.updated` after `conversation.draft.set` and after queued sidebar mentions mutate an inactive conversation draft.
- Why it matters: frontend composer drafts already hydrate from metadata and handle `draft_update`; the Rust server must emit the live notification whenever it persists a draft so other clients stay in sync without polling.
- Citations: `rust/crates/als-server/src/conversation_rpc.rs`; `rust/crates/als-server/src/sidebar_ipc.rs`; `agent_log_server/static/js/codex_agent/events/router.ts`; `agent_log_server/static/js/codex_agent/composer/runtime.ts`
### ALS-RS conversation list sync

- Fact: ALS-RS broadcasts backend-owned `conversation.list.updated` snapshots with monotonic `revision` values after create/select/update/session-bind/delete/pin and provider metadata mutations; frontend clients replace splash/minibar list state from the snapshot and drop stale revisions.
- Why it matters: cross-client conversation drawer/splash state is Rust-owned instead of reconstructed independently by each browser client. Future conversation-list work should emit this snapshot for accepted structural/meta mutations and keep high-frequency draft typing on `conversation.draft.updated`.
- Citations: `rust/crates/als-server/src/conversation_rpc.rs`; `rust/crates/als-server/src/state.rs`; `agent_log_server/static/js/codex_agent/rpc/conversations/contract.ts`; `agent_log_server/static/js/codex_agent/events/router.ts`
## Runtime and protocol behavior

### Codex protocol patch reporting

- Fact: Codex app-server exposes `apply-patch` outcome via `FileChangeThreadItem.status` and legacy `patch_apply_end` status/success/stderr/stdout fields.
- Why it matters: patch status normalization needs to read both the structured item field and the legacy payload fields for compatibility.
- Citations: `worktrees/app-server-schema/json/codex_app_server_protocol.v2.schemas.json:3912-3940,11183-11224,3434-3441`

### Codex initialize contract

- Fact: `codex-ext` must negotiate `initialize.capabilities.experimentalApi` from the runtime schema to use `turn/start.collaborationMode` successfully.
- Why it matters: collaboration mode is gated by initialize capability negotiation, so request-shape fixes alone are not enough.
- Citations: `extensions/codex_ext/runtime_protocol.py:1033-1077`; `extensions/codex_ext/transport.py:408-421`; `worktrees/app-server-schema/json/v1/InitializeParams.json:9-18,45-53`

### ACP session resume vs load

- Fact: ACP `session/resume` is an unstable no-history continuation path, while `session/load` is the history-hydration path; Gemini resume should bind with resume first and use load for transcript hydration.
- Why it matters: Gemini/ACP session picker flows cannot use one ACP call for both live continuation and transcript reconstruction. Future ACP session work should keep `use_unstable_protocol=True` for resume-capable clients and reserve `load_session` for transcript hydration/replay.
- Citations: `worktrees/gemini_acp_extension/transport.py:464-507,622-653,753-758`; `worktrees/gemini_acp_extension/client.py:338-424`; `worktrees/gemini_acp_extension/_vendor/python-sdk/schema/schema.json:981-1008,3214-3334`; `worktrees/gemini_acp_extension/_vendor/python-sdk/src/acp/core.py:75-98`

### Existing harness conversation reload is transcript-first

- Fact: Reloading or selecting an existing harness conversation hydrates the UI from the harness-owned `transcript.jsonl`; the bound extension session stays cold until the first new send, which then performs the lazy resume/retry path.
- Why it matters: this repo distinguishes harness transcript replay from live extension session loading. Future session-resume work must not eagerly attach extension sessions on conversation reload/select, and any resume-time history noise must not replace or duplicate the already-hydrated harness transcript.
- Citations: `agent_log_server/server.py:7256-7398,7573-7588`; `extensions/codex_ext/client.py:1212-1263`; `User input: Yeah I'm not talking about "loading a conversation from Gemini's harness" I'm talking about "loading a conversation that has already been started on our harness and who's sessionid is bound to a conversationID."`; `User input: The UI is populated with cards from the previous conversation.... That's it, it doesn't actually 'load a session'`

### Transcript tail/history gate

- Fact: The transcript treadmill now uses a one-way "Egyptian scroll" model: while pinned, live tail growth clips head rows to `transcriptLimit`; while free-scrolling, head clipping pauses but live tail appends continue; repinning collapses the mounted transcript back to the latest `transcriptLimit` window; the head spacer only disappears when `transcriptStart === 0`.
- Why it matters: transcript rendering no longer uses the older bidirectional tail/history-resume contract. Future treadmill work should preserve always-appending live tail behavior during free-scroll and treat `transcriptHistoryMode` as the expanded/free-scroll state, not as a reason to suppress visible live transcript events.
- Citations: `agent_log_server/static/codex_agent.ts`; `agent_log_server/static/js/codex_agent/transcript_loader.ts`; `agent_log_server/static/js/codex_agent/boot/input_flow.ts`; `agent_log_server/static/js/codex_agent/transcript_config.ts`

### Codex response schema registry

- Fact: Codex runtime response schemas are keyed by lowercase method names in `runtime_protocol`, and missing response entries can be rebuilt from the loaded schema definitions / schema bundle on demand.
- Why it matters: protocol additions can fail silently if a mixed-case method key is registered or a cached runtime protocol lacks a newer response entry. Future response-method work should stay schema-driven, use lowercase registry keys, and rely on the runtime protocol’s rebuild path instead of ad hoc payload parsing.
- Citations: `extensions/codex_ext/runtime_protocol.py:126-140,596-705,713-797,1463-1476`; `extensions/codex_ext_exp/runtime_protocol.py:126-140,612-721,729-813,1479-1492`; `User input: Look at the shells and look at the schema and the TS bindings, use the SCHEMA for the semantics and typing.`

### Codex config

- Fact: Codex ThreadStartParams and ThreadResumeParams allow open-ended config objects plus cwd/developerInstructions, enabling per-thread MCP config injection.
- Why it matters: per-thread config flexibility means extensions can inject MCP servers, developer instructions, and cwd at thread start/resume time without protocol changes.
- Citations: `worktrees/app-server-schema/json/v2/ThreadStartParams.json:22-40`; `worktrees/app-server-schema/json/v2/ThreadResumeParams.json:26-44`; `/data/data/com.termux/files/home/downloads/codex/codex-rs/app-server/README.md:186-199,229-231`

### Auth flow behavior

- Fact: Codex ChatGPT auth is notification-driven: open `authUrl` after `account/login/start`, then wait for `account/login/completed` and `account/updated`; do not long-poll `account/read` while login is pending.
- Why it matters: polling during login caused bad behavior before, so auth completion should continue to follow the event/notification flow.
- Citations: `/data/data/com.termux/files/home/downloads/codex/codex-rs/app-server/README.md:1256-1261,1311-1320`; `/data/data/com.termux/files/home/downloads/codex/codex-rs/app-server/tests/suite/v2/account.rs:918-977`

### Browser opener in Termux

- Fact: In this Termux environment, prefer `xdg-open` as the canonical browser opener; it acts as the compatibility shim the user expects.
- Why it matters: auth/login and browser-opening flows should use the environment behavior the user already validated instead of substituting a different opener.
- Citations: `User input: okay yeah so the termux xdg open is a compatibility shim it works the same basically that's why I was adamant about just using that`

### TE2 console eval pacing
- Fact: The 5-second wait before `te2_console_eval` only applies to introspective TE2/harness workers such as `codex_agent` and `main_page`, where the user may need to switch the visible target first. It is not required for app-specific workers with distinct labels such as `als_rs`.
- Why it matters: the wait is a workaround for self-inspecting harness surfaces, not a blanket rule for all TE2 console evals. App-specific workers can be evaluated directly by exact worker ID, which keeps validation fast and avoids unnecessary sleeps.
- Citations: `User input: The five second rule is for the codex_agent and main_page workers... The reason being is those are introspective because that's basically the console bridge looking at itself.`; `User input: You don't actually have to run it here. You can even change it in the agent's file to whatever, there is no need for a 5-second rule on this repo.`
### Context compaction routing

- Fact: Context compaction is initiated by a generic SIO `compact` event from the frontend; `server.py` routes it through `ext_loader.compact_session()` for extension-owned conversations. Codex extensions use `thread/compact/start` RPC (must be in `_CLIENT_RESPONSE_METHODS`); copilot-sdk uses `session.rpc.compaction.compact()`.
- Why it matters: compact was broken because `thread/compact/start` was missing from the schema whitelist and the server routed all compact requests to the legacy builtin app-server. Both the schema registration and the routing must be correct for compaction to work.
- Citations: `extensions/codex_ext/runtime_protocol.py:52`; `extensions/copilot_sdk/client.py:2493-2507`; `extensions/__init__.py:1375-1380`; `agent_log_server/server.py:8000-8049`

### Patched codex app-server binary

- Fact: A patched `codex-app-server` binary exists at `~/downloads/codex/codex-rs/target/debug/codex-app-server` (branch `patch/dynamic-developer-instructions`, base `rust-v0.117.0-alpha.5`). It adds `developer_instructions: Option<Option<String>>` to `Op::OverrideTurnContext`, `SessionSettingsUpdate`, and `TurnStartParams`, enabling mid-thread developer instruction updates.
- Why it matters: The stock `codex app-server` locks developer instructions at thread start. This patched binary is required for `codex-ext-exp` to support dynamic context injection (the pending_context system). `codex-ext-exp` must use its own shellspec pointing at this binary, not the stock `codex` CLI.
- Citations: `~/downloads/codex/codex-rs/protocol/src/protocol.rs:350-356`; `~/downloads/codex/codex-rs/core/src/codex.rs:1132-1135,1152,4206-4231`; `~/downloads/codex/codex-rs/app-server/src/codex_message_processor.rs:6012-6035`; `~/downloads/codex/codex-rs/app-server-protocol/src/protocol/v2.rs:3881-3893`; `worktrees/notes/PATCHED_APP_SERVER.md`

### Copilot assistant.message envelopes

- Fact: Copilot SDK can emit empty `assistant.message` tool-request envelopes; only `assistant.message` events with real content should finalize transcript cards before tool rows.
- Why it matters: replay fidelity depends on keeping pre-tool assistant prose ahead of MCP/tool transcript rows without reintroducing blank assistant cards from empty tool-envelope messages.
- Citations: `/data/data/com.termux/files/home/.cache/app_server/conversations/af0d6d1044cc4f27a8db4ef643db719b/transcript.jsonl:333,525`; `extensions/copilot_sdk/router.py:717-770`

### Resume virtual ack timing
- Fact: Codex conversation resume still follows the lazy `turn/start -> thread/resume -> turn/start` fallback, but the `thread/resume` virtual-ack leg can complete well after 10 seconds while app-server startup/auth noise is still settling; both `codex-ext` and `codex-ext-exp` now wait 30 seconds for that resume leg.
- Why it matters: if this timeout is shortened or misdiagnosed as a bind-only problem, message send fails before the retrying second `turn/start` even though the app-server does eventually emit `thread/status/changed idle` and the successful `thread/resume` result.
- Citations: `extensions/codex_ext/client.py:432-451`; `extensions/codex_ext_exp/client.py:597-616`; `/data/data/com.termux/files/home/.cache/framework_shells/runtimes/b4f2d44683b344a8/0a37fcbc242ec454/logs/fs_1775079288_04cef54f.stderr.log:25-27,48,67`; `User input: Okay, everything seems to be working.`
### FWS pipe reader contract
- Fact: Pipe-backed shell consumers in this repo must treat framework-shells as the owner of stdout consumption: use `subscribe_output()` / `subscribe_output_bytes()` for reads and `write_to_pipe()` for stdin writes, not direct `state.process.stdout.read(...)` loops.
- Why it matters: `framework_shells` now actively consumes and tee-logs pipe stdout itself. Concurrent direct reads trigger `RuntimeError("read() called while another coroutine is already waiting for incoming data")` and can break Codex app-server transports or the legacy builtin app-server reader.
- Citations: `framework_shells/manager.py:879-915`; `framework_shells/manager.py:1692-1785`; `extensions/codex_ext/transport.py:422-640`; `extensions/codex_ext_exp/transport.py:360-640`; `agent_log_server/server.py:5514-5687`; `User input: stacktrace showing read() called while another coroutine is already waiting for incoming data`
### Conversations RPC live slice
- Fact: `/rpc/conversations` now owns the full conversation transport: CRUD, update/draft, replay, send/interrupt/compact, and live conversation notifications. Legacy `/appserver` no longer carries conversation request handlers or overlapping live conversation `appserver_event` traffic.
- Why it matters: future conversation work should extend the conversations namespace directly instead of preserving the old dual-lane compatibility path. The splash RPC toggle is no longer a conversation-lane selector, and the old duplicate-suppression handshake should not be reintroduced.
- Citations: `agent_log_server/appserver_routes.py`; `agent_log_server/server.py`; `agent_log_server/appserver_socketio.py`; `agent_log_server/static/js/codex_agent/rpc/conversations/client.ts`; `agent_log_server/static/js/codex_agent/events/socket.ts`

### Conversations live ownership handshake
- Fact: The old `/appserver` conversations live-ownership handshake was removed when conversation live delivery became RPC-only.
- Why it matters: there is no longer a supported reason to suppress overlapping conversation events on `/appserver`, because those overlapping conversation live events should not be emitted there at all. If duplicate conversation rendering reappears, the fix belongs in the RPC/live emit path rather than by reviving the ownership handshake.
- Citations: `agent_log_server/appserver_socketio.py`; `agent_log_server/server.py`; `agent_log_server/static/js/codex_agent/events/socket.ts`; `te2_services/appserver_transport.py`
### Copilot schema modal reasoning fallback
- Fact: Copilot model-list RPC can truncate GPT-family reasoning metadata into `supportedReasoningEfforts = ["low","medium","high"]` even when the same model advertises a fuller set like `xhigh` under `capabilities.supports.reasoning_effort`. The extension-side `list_models()` surface must merge that nested capabilities list so shared settings UIs render the real supported efforts.
- Why it matters: both the shared schema modal and the main settings UI consume the extension model list. If the extension publishes the truncated field verbatim, `xhigh` disappears before either UI sees it and the dropdowns under-report the model's reasoning options.
- Citations: `extensions/copilot_sdk/client.py`; `extensions/copilot_sdk/_vendor/copilot/types.py`; `/data/data/com.termux/files/home/.cache/framework_shells/runtimes/b4f2d44683b344a8/0a37fcbc242ec454/logs/fs_1776555895_d5cc02b3.stdout.log`
### Copilot compaction RPC drift
- Fact: On the updated Copilot CLI/runtime, the vendored SDK's `session.compaction.compact` request is rejected with `-32601 Unhandled method`, even though the runtime still emits `session.compaction_start` events. The current `copilot-sdk` compaction call path is therefore stale against the runtime contract.
- Why it matters: harness compaction for Copilot will keep failing until the extension stops using the generated SDK method verbatim and aligns with the updated runtime contract. This is separate from Codex and separate from the modal bug.
- Citations: `extensions/copilot_sdk/client.py`; `extensions/copilot_sdk/_vendor/copilot/generated/rpc.py`; `/data/data/com.termux/files/home/.cache/framework_shells/runtimes/b4f2d44683b344a8/0a37fcbc242ec454/logs/fs_1776555895_d5cc02b3.stdout.log`
### Copilot SDK upstream drift after 922959f
- Fact: Updating `worktrees/copilot-sdk` from `396e8b3c04175dcf2fd1c7c34950c3fc0a5395e8` to `922959f4a7b83509c3620d4881733c6c5677f00c` breaks the local FWS stdio transport quilt patch verbatim: `python/copilot/types.py` no longer exists upstream, and the `python/copilot/client.py` hunk no longer applies. The captured pre-pull artifact lives at `worktrees/copilot_sdk_quilt_2026-04-18/0001-fws-stdio-process-injection.diff`.
- Why it matters: the Copilot SDK vendor update cannot be copied into `extensions/copilot_sdk/_vendor` blindly. The custom external-process transport patch now needs a real rebase against the new upstream Python SDK layout before any vendoring.
- Citations: `worktrees/copilot_sdk_quilt_2026-04-18/0001-fws-stdio-process-injection.diff`; `worktrees/copilot-sdk/python/copilot/client.py`; `worktrees/copilot-sdk/.git`
### Copilot SDK vendoring after 922959f rebase
- Fact: The rebased FWS stdio transport patch now lives as a real quilt patch at `worktrees/copilot_sdk_rebase_quilt_2026-04-18/0001-fws-stdio-process-injection.patch` against upstream `922959f4a7b83509c3620d4881733c6c5677f00c`. The vendored Python SDK in `extensions/copilot_sdk/_vendor/copilot` was refreshed from that patched worktree, and the extension-side client was updated to use the new upstream Python API (`SubprocessConfig`, keyword `create_session`/`resume_session`, and `session.send(prompt, attachments=...)`).
- Why it matters: the old local transport patch no longer fit upstream after the SDK reorg. Future Copilot SDK updates need to treat the quilt patch and the extension-side API adaptation as a pair rather than assuming the old `types.py`/dict-options surface still exists.
- Citations: `worktrees/copilot_sdk_rebase_quilt_2026-04-18/0001-fws-stdio-process-injection.patch`; `extensions/copilot_sdk/_vendor/copilot/client.py`; `extensions/copilot_sdk/client.py`
### Copilot SDK vendoring after f625779 rebase
- Fact: `extensions/copilot_sdk/_vendor/copilot` was refreshed from upstream Copilot SDK commit `f625779` with the local FWS stdio transport patch rebased as `worktrees/copilot_sdk_rebase_quilt_2026-05-11/0001-fws-stdio-process-injection.patch`. The extension protocol adapter reads complete `assistant.reasoning` text from either `reasoning_text` or the generated `content` field.
- Why it matters: future Copilot SDK refreshes should pull upstream first, rebase the stdio transport patch, then vendor the patched Python SDK. The reasoning adapter fallback preserves complete reasoning events when the runtime sends `assistant.reasoning` without deltas.
- Citations: `extensions/copilot_sdk/_vendor/copilot/client.py`; `extensions/copilot_sdk/_vendor/copilot/generated/session_events.py`; `extensions/copilot_sdk/protocol_adapter.py`; `worktrees/copilot_sdk_rebase_quilt_2026-05-11/0001-fws-stdio-process-injection.patch`
### agent-pty-blocks HTTP shim
- Fact: `mcp_agent_pty_http_shim.py` provides a standalone streamable-HTTP shim for `agent-pty-blocks`; it keys child MCP subprocesses by `conversation-id`, passes `CONVERSATION_ID` and `PWD` into `mcp_agent_pty_server.py`, and reverse-proxies `/mcp` to the per-conversation child.
- Fact: `codex-ext` and `codex-ext-exp` now inject `agent-pty-blocks` as streamable HTTP at `http://127.0.0.1:8765/mcp` by default (overridable with `AGENT_PTY_HTTP_SHIM_URL`), with `http_headers.conversation-id` and `http_headers.x-agent-cwd`; stdio `command`/`args`/`env`/`cwd` keys are stripped from the injected entry.
- Why it matters: Codex's streamable-HTTP MCP inventory appears more reliable than stdio in current debugging, while the shim preserves the existing env-backed `ask_user`/KB identity semantics without refactoring every MCP tool to be request-context-aware.
- Citations: `mcp_agent_pty_http_shim.py`; `mcp_agent_pty_server.py`; `extensions/codex_ext/runtime_protocol.py`; `extensions/codex_ext_exp/runtime_protocol.py`; smoke test listed 31 tools including `ask_user` and `kb_write` through the shim.
## Build and validation

### Frontend validation commands

- Fact: Frontend validation uses `npm run typecheck --quiet` and `npm run build --quiet`, and source edits under `static/` should not be treated as live until the frontend has been rebuilt.
- Why it matters: this is the repo-standard validation pair for frontend-affecting changes, and it prevents debugging against stale served assets when source files have changed but the delivered frontend has not.
- Citations: `package.json:9-13`; User input: Oh because you didn't build the source when you were done

### Frontend TypeScript contract

- Fact: Frontend TypeScript now runs with `strict: true`, and `npm run typecheck --quiet` also enforces `@typescript-eslint/no-explicit-any` on first-party `agent_log_server/static/**/*.ts` files.
- Why it matters: future frontend work has to satisfy real strict typing and cannot rely on explicit `any` in the first-party TS surface. This keeps new typing cleanup aligned with the repo's enforced contract instead of treating strict/no-explicit-any as a temporary migration goal.
- Citations: `tsconfig.json:2-21`; `package.json:5-17`; `eslint.config.mjs:11-24`

### Build pipeline

- Fact: `npm run build` vendors JetBrains Mono, Highlight.js, xterm, and streaming-markdown into `agent_log_server/static/vendor`.
- Why it matters: frontend asset changes often require rebuilding vendored artifacts, not just editing source files under `static/`.
- Citations: `build.mjs:29-150`; `agent_log_server/server.py:5962-5969`; `package.json:14-18`

### Repo basedpyright config
- Fact: Repo-level `basedpyright` config now lives in `pyproject.toml`. It scopes analysis to first-party code (`*.py`, `agent_index`, `agent_log_server_rs`, `extensions`), excludes `als_deprecated`, `worktrees`, `build`, `node_modules`, `als_deprecated.egg-info`, and `extensions/copilot_sdk/_vendor`, and downgrades `reportMissingImports` to a warning for environment-specific local runtime packages like `framework_shells`.
- Fact: After the May 2026 active-code typing cleanup, repo-wide `basedpyright --outputjson` reports only 18 remaining errors, all in deprecated removal candidates: `agent_index.py` (11) and `shell_manager.py` (7).
- Why it matters: future typing work should use the repo-owned config instead of relying on editor-local settings. This keeps repo-wide basedpyright output focused on first-party code; active ALS-RS/extension/MCP code is clean, while the remaining baseline should be handled by pruning or excluding the deprecated files rather than polishing them.
- Citations: `pyproject.toml:48-66`; `basedpyright --outputjson` run on 2026-05-18 reported `18 errors, 0 warnings` only in `agent_index.py` and `shell_manager.py`.

### Basedpyright invocation
- Fact: In this repo, run `basedpyright` from `PATH` directly and do not invoke it through `npx --no-install`.
- Why it matters: validation commands should use the installed executable the user expects in this environment instead of node-based indirection. This keeps typecheck commands aligned with the repo's local tool setup and avoids repeating the wrong validation pattern in future tasks.
- Citations: `AGENTS.md`; `User input: Stop using npx basedpyright. Just use the one in PATH.`

### Copilot harness heredoc quirk
- Fact: In this Copilot harness, do not combine a Python heredoc command with other chained scripts/commands in the same Bash invocation. Run the heredoc in its own shell call, or use `python -c` for mixed validation chains.
- Why it matters: combined heredoc + additional scripts can hang and withhold otherwise-finished command output, which makes validation/debugging look stuck when the real issue is the shell wrapper pattern. Future validation and investigation commands should keep heredocs isolated so command output returns reliably.
- Citations: `AGENTS.md:232-235`; User input: Yeah I was waiting for you to figure that out (when your validation was hanging) But it looks like you did... The copilot harness doesn't like parallel heredocs with other scripts.
## Search discipline

### Do not blindly content-search bundled or generated trees

- Fact: Do not run blind recursive file-content searches through high-noise bundled/generated roots in this repo; use name-only listing there and narrow to specific source trees before searching contents. The main no-blind-search zones are `node_modules/`, `build/`, `scripts/schema-extract/node_modules/`, `worktrees/**/build/`, and `agent_log_server/static/dist/`, plus obvious bundled artifacts like `*.min.js`, `*.bundle.js`, and `*.map`.
- Why it matters: these paths contain minified bundles, generated assets, sourcemaps, and vendor code that drown out real matches and waste investigation time. Future debugging and code review work needs a durable reminder to search source intentionally instead of spraying `rg` across the highest-noise trees in the repo.
- Citations: `AGENTS.md:177-198`; `agent_log_server/static/dist/codex_agent.js` (200927 bytes); `agent_log_server/static/dist/codex_agent.js.map` (703952 bytes); `agent_log_server/static/vendor/markdown-it/markdown-it.min.js` (123524 bytes); `agent_log_server/static/vendor/socket.io/socket.io.min.js` (49993 bytes); `scripts/schema-extract/node_modules/@ts-morph/common/dist/typescript.js` (9111641 bytes); `node_modules/@xterm/xterm/lib/xterm.js.map` (1924267 bytes); User input: The repo root is good example of where NOT to search for file contents, also agent_log_server/ (the pyproject root)

### Use Python heredoc heuristics for conversations and framework-shell logs

- Fact: For conversation transcripts and framework-shell logs, do not blindly `rg`/`grep` file contents. File-name/path discovery is fine, but content inspection should use targeted Python heredoc heuristics against `~/.cache/app_server/conversations/` and `~/.cache/framework_shells/runtimes/**/logs/`.
- Why it matters: those caches are structured, noisy, and often contain large JSON/log streams where raw grep misses boundaries or floods the result set. Future agents need the explicit rule to use Python parsing/scoping for these roots so debugging stays precise and does not regress into blind log scraping.
- Citations: `AGENTS.md:189-198`; User input: Also we need to call out how to search conversations and framework shell logs with python heredoc heuristics ... and to also not blindly rg/grep them as well. A regular list file NAME search in these directories is, Just not a file CONTENT search

## Documentation anchors

### Deprecated documentation anchors

- Fact: `CODEX_APP_SERVER_EXTENSION.md` is deprecated, and `CODE_TE2.md` is also deprecated though somewhat less so; neither should be treated as a primary KB discovery anchor.
- Why it matters: agents should not use old Python/Codex-era docs as the first source of truth for ALS-RS work. The KB file list now drops both deprecated docs and includes `worktrees/notes/ALS_RS_MIGRATION_PLAN.md` as the active migration planning anchor.
- Citations: `User input: The info in CODEX_APP_SERVER_EXTENSION.md is deprecated, obviously, also CODE_TE2.md But a little less so`; `.agent-pty.toml`

### Extension integration guide

- Fact: AGENT_EXTENSION_INTEGRATION.md is the canonical guide for building and maintaining pluggable agent extensions.
- Why it matters: This is the repo’s canonical extension-architecture guide even though it lives under the historical acp/ path. Remembering it will help future extension work stay aligned with the generic hook surface, ownership boundaries, and the current Codex/Copilot reference patterns.
- Citations: `AGENT_EXTENSION_INTEGRATION.md:1-12,389-542`

### Session lifecycle contract docs

- Fact: The canonical owner for the extension session lifecycle contract is `AGENT_EXTENSION_INTEGRATION.md`; `AGENTS.md` holds the short repo invariant, and `extensions/__init__.py` docstrings mirror the generic hook semantics.
- Why it matters: This contract is broader than transcript card shape — it defines transcript-first reload of existing harness conversations versus port-in/import flows and the lazy first-send resume/retry behavior for bound remote sessions. Future extension work should update the canonical integration guide first, then keep the guardrail/integration-manual/code-docstring mirrors in sync without treating deprecated docs as authoritative.
- Citations: `AGENT_EXTENSION_INTEGRATION.md:14-70,231-240,687-705`; `AGENTS.md:129-143`; `extensions/__init__.py:2494-2536`

### Transcript card contract guide

- Fact: TRANSCRIPT_CARD_CONTRACTS.md defines the generic live/transcript card contracts and replay-parity rules for routers and frontend code.
- Why it matters: This contract doc governs how routers and the frontend preserve generic card behavior and `_emit()`/`_record()` parity across live and replay. Storing it will help future transcript, replay, and tool-card work avoid extension-specific drift and contract regressions.
- Citations: `TRANSCRIPT_CARD_CONTRACTS.md:1-17,276-316,374-398`

### Apply-patch diff rendering contract

- Fact: Apply-patch/file-change tool cards are summary/outcome/path cards; standalone `diff` cards own visible patch body rendering, even when related tool events or transcript rows carry `diff` metadata.
- Why it matters: Codex and Copilot routers can preserve provider metadata on tool rows while keeping multi-file patch rendering replay-safe and path-specific through the generic `diff` contract. Future router or frontend changes should not reintroduce embedded apply-patch diff rendering as the primary visible lane.
- Citations: `TRANSCRIPT_CARD_CONTRACTS.md:354-380,641-649,670-675`; `extensions/codex_ext/router.py:1220-1239,2074-2162,2416-2424`; `extensions/copilot_sdk/router.py:1537-1579`; `rust/crates/als-server/src/static/js/codex_agent/tool_render.ts:367-371,630-634`

### Approval request-card manifest contract

- Fact: Extension `manifest.json` `ui.requestCards[]` declarations own custom approval/request-card modules. ALS-RS exposes them through `extension.requestCards.get`, serves modules from extension-local `ui/...` or `static/...` assets, matches by request method/kind, and passes helpers including the shared approval diff renderer.
- Why it matters: request-card rendering is a manifest/schema-owned extension UI contract, not provider-specific frontend bundle wiring. Future approval-card work should keep module discovery generic and render approval diffs through the shared helper while standalone transcript patch bodies remain `diff` rows.
- Citations: `APPROVAL_CONTRACT.md:29-89`; `EXTENSIONS_CONTRACT.md:146-183`; `rust/crates/als-server/src/settings_rpc.rs:100-111,1075-1113`; `rust/crates/als-server/src/static_assets.rs:23-25,136-149`; `rust/crates/als-server/src/static/js/codex_agent/request_cards/runtime.ts:62-74,117-229`; `rust/crates/als-server/src/static/js/codex_agent/approvals/ui.ts:676-689`; `extensions/codex_ext/manifest.json:109-134`; `extensions/copilot_sdk/manifest.json:109-133`

### Terminal ANSI rendering

- Fact: Shell/command cards use the shared frontend `terminal_ansi.ts` helper to render ANSI SGR color/style sequences only when output contains those escape codes; non-ANSI output keeps the existing text/highlight path.
- Why it matters: provider extensions can emit plain command output or ANSI-colored command output through the same generic shell/command event contracts without extension-specific render branches.
- Citations: `agent_log_server/static/js/codex_agent/terminal_ansi.ts`; `agent_log_server/static/js/codex_agent/shell_render.ts`; `agent_log_server/static/js/codex_agent/transcript_cards.ts`; `agent_log_server/static/js/codex_agent/timeline/replay.ts`

## Repo memory harness

### Canonical repo memory backing file

- Fact: Repo memory is backed by `.repo_memory.md`, and the shared prompt-context loader discovers it from the logical repo root before appending it to developer instructions.
- Why it matters: repo memory injection is now a real runtime seam, so future memory-related work should update this file-backed path instead of inventing a second source.
- Citations: `agent_log_server/prompt_context.py:9-20,34-103,123-143`

### KB registration

- Fact: `.repo_memory.md` is registered in the KB config so the KB tool surface and prompt-injection surface point at the same backing file.
- Why it matters: editing repo memory through KB and injecting repo memory into prompts now target the same durable artifact.
- Citations: `.agent-pty.toml:4-5`

### KB root

- Fact: KB root now follows the harness launch cwd/repo root; `kb_reload_config()` no longer switches repos via cwd override.
- Why it matters: KB scope is tied to the active project root at launch, so agents should not attempt to re-scope KB to a different repo mid-session.
- Citations: `mcp_agent_pty_server.py:63-95,3301-3364`; `extensions/copilot_sdk/te2_runtime.py:15-59`; `KB_MEMORY_MCP.md:25-28,105-106,160`

### KB mutation model

- Fact: KB mutations are patch-style edits using `target` + `section`; mutation section selectors match the read/info selector model and accept schema numbers, `L<line>` / `line:<line>`, heading paths, unique titles/suffixes, or `section=""` for the file root. KB write/update/remove do not emit repo-memory IPC notifications. `kb_write` heading creation defaults to `spacing="auto"` so generated Markdown headings get safe blank-line spacing and a final newline, with `spacing="preserve"` available for exact raw insertion.
- Why it matters: agents should not add extra compare-and-swap bookkeeping for normal KB writes or diagnose stale pending-context IPC noise after a successful durable file patch. Use `dry_run` when a preview is useful, treat the KB mutation result as file-write-only, and rely on structured heading writes to preserve Markdown section spacing unless exact insertion was requested.
- Citations: `mcp_agent_pty_server.py`; `KB_MEMORY_MCP.md`; `AGENTS.md:238-243`

### Runtime helpers

- Fact: Shared prompt-context helpers live in `agent_log_server/prompt_context.py` and TE2 MCP config helpers live in `agent_log_server/te2_mcp_config.py`; `te2_runtime.py` is a compatibility shim.
- Why it matters: new prompt-composition or TE2 config work should target these shared helpers instead of duplicating logic per extension or inventing new paths.
- Citations: `agent_log_server/prompt_context.py:1-152`; `agent_log_server/te2_mcp_config.py:1-73`; `agent_log_server/te2_runtime.py:1-31`; `agent_log_server/server.py:35-44`

### Prompt injection seams

- Fact: the shared prompt-context helper is consumed by builtin Codex in `server.py`, and the same helper is wired into `codex-ext` and Copilot runtime/session config paths.
- Why it matters: repo memory now flows through every active agent path, so changes to prompt composition should be made in the shared helper instead of duplicated per extension.
- Citations: `agent_log_server/server.py:5297-5379`; `extensions/codex_ext/runtime_protocol.py:1142-1148`; `extensions/copilot_sdk/client.py:941-949,994-1002`

### Repo memory debug preview

- Fact: `GET /api/appserver/repo_memory` returns the resolved repo-memory snapshot for the active conversation or cwd.
- Why it matters: this gives a backend-level way to verify what memory text the harness is actually resolving before debugging agent behavior.
- Citations: `agent_log_server/server.py:6763-6787`

### KB adoption hooks in developer template

- Fact: `DEVELOPER_MESSAGE_TEMPLATE.md` contains a "Repo Knowledge Base (KB)" section with current behavioral hooks: call `kb_list`, `kb_help`, and target-file `kb_schema(file="...")` / `kb_schema(target="...")` for the complete numbered ATX heading index; use `kb_info` for parent-chain/range/preview context; use `kb_read` for selected bodies; use schema numbers, heading paths, `L<line>` / `line:<line>`, or unique title/suffix selectors; use `target` + `section` for KB mutation tools; use `create_child` / `child` for child-heading writes; and treat KB mutations as file-write-only with no repo-memory IPC notification.
- Why it matters: Agents were not discovering or using KB tools because the instructions were buried in platform-level custom_instructions and were missing current mode/discovery semantics. The template injection is the guaranteed delivery path — every extension conversation gets it. KB adoption depends on these hooks being accurate in the template.
- Citations: `DEVELOPER_MESSAGE_TEMPLATE.md:112-135`; `KB_MEMORY_MCP.md:40-90,113-119,138-142,166-171`; `agent_log_server/prompt_context.py:123-143`; `extensions/copilot_sdk/client.py:1146-1154`

### Pending context watcher

- Fact: `pending_context.py` is a new module providing inotify-based file watching for `.repo_memory.md` changes. It auto-queues `PendingContextUpdate` entries for matching conversations (scoped to `codex-ext-exp` agent type + matching `cwd`). Wired into `server.py` startup/shutdown at lines 101-111 and 134-135.
- Why it matters: This is the detection layer for dynamic mid-thread developer instruction updates. When `.repo_memory.md` changes, the watcher queues updates that the codex-ext-exp client will pop at the next `turn/start` to send updated `developerInstructions` to the patched binary.
- Citations: `agent_log_server/pending_context.py`; `agent_log_server/server.py:46,101-111,134-135`

### Dynamic devins canary test

- Fact: CANARY_DYNAMIC_DEVINS_TEST_8824: Fresh post-compaction test memory for validating that codex-ext-exp receives a mid-session repo-memory update.
- Why it matters: If the live experimental conversation can identify this exact canary after it was added mid-session, the watcher, queue, and turn-start developerInstructions pipeline are all working with the full prompt context.
- Citations: `agent_log_server/pending_context.py`; `extensions/codex_ext_exp/client.py`; `agent_log_server/prompt_context.py`

### Sticky headers

- Fact: Large or unbounded transcript cards should participate in sticky headers so users do not get lost while scrolling their content.
- Why it matters: when content becomes long, sticky headers are part of the intended UX contract rather than an optional styling detail.
- Citations: `User input: Yeah if it's going to be unbound like that, then we have to have a sticky header. Otherwise the user can get lost inside of the content from it being so large.`
### Do not ask to restart harness server
- Fact: Do not ask to restart the live harness server in this repo; the active agent session is running on that server, and restarting it kills the harness connection.
- Why it matters: ask-user and live debugging work stall if the agent tells the user to restart the very backend the harness depends on. In this setup, restart requests are off-limits unless the user explicitly chooses that disruption.
- Citations: `mcp_agent_pty_server.py`; `User input: STOP ASKING ME TO RESTART THE SERVER, YOUR HARNESS IS ON THIS SERVER. ADD THAT TO THE REPO MEMORY`; `User input: DONT EVER RESTART THE GODDAMN LIVE FUCKING SERVER... I DON'T KNOW HOW MANY TIMES I HAVE TO TELL YOU THIS SHIT... THAT'S THE DAMN HARNESS YOU'RE RUNNING ON MAN`

### Keep local harness rules repo-scoped
- Fact: Workspace-local harness rules in this repo (for example not restarting the live harness server) belong in repo-local docs/memory only and must not be copied into downstream consumer or third-party extension guidance.
- Why it matters: downstream-facing docs should describe portable install/update behavior for consumers, while this repo's live-harness constraints are specific to how the agent session is hosted here. Mixing those scopes leaks local assumptions into external guidance and produces the wrong instructions.
- Citations: `User`
### KB section shorthand and file-root targeting
- Fact: KB section resolution now accepts schema numbers, full heading paths, unique visible heading titles, unique trailing path suffixes, and `L<line>` / `line:<line>` selectors; mutation tools use `section=""` as the file-root target.
- Why it matters: agents can patch markdown knowledge files ergonomically from the schema/info/search output, while file-root writes remain explicit and readable.
- Citations: `mcp_agent_pty_server.py`; `KB_MEMORY_MCP.md`
## Markdown link routing

### Markdown file links route through TE2 open

- Fact: Markdown-rendered file-path links in transcript cards are intercepted in the frontend and routed through the same `te2_agent_open` Socket.IO flow used by TE2 diff/view cards, not left as plain browser navigation.
- Why it matters: file links in assistant/user markdown now open in the host editor with the existing sidebar/IDE pipeline instead of navigating the iframe away from the transcript.
- Citations: `agent_log_server/static/js/codex_agent/markdown.js:109-142`; `agent_log_server/static/codex_agent.js:1894-1953`; `agent_log_server/server.py:993-1003`

### Markdown external links use backend xdg-open

- Fact: Markdown-rendered `http/https` links are intercepted in the frontend and sent over Socket.IO to backend `open_external_url`, which validates the URL and launches it with `xdg-open`.
- Why it matters: external citations now open through the device/browser opener path without adding HTTP fallback routes or relying on default iframe anchor navigation.
- Citations: `agent_log_server/static/js/codex_agent/markdown.js:120-125`; `agent_log_server/static/codex_agent.js:1924-1953`; `agent_log_server/server.py:1006-1045`

### Markdown file links parse line and column metadata

- Fact: Markdown file links support `#L123`, `#L123C4`, and `path:line[:column]` parsing before dispatch, and the parsed `{ path, line, column }` target is forwarded into the TE2 open request.
- Why it matters: rendered file references preserve editor jump location instead of sending a raw `#L...` suffix as part of the file path.
- Citations: `agent_log_server/static/js/codex_agent/markdown.js:8-33`; `agent_log_server/static/js/codex_agent/markdown.js:64-98`; `agent_log_server/static/js/codex_agent/markdown.js:127-132`; `agent_log_server/static/codex_agent.js:1941-1948`

### Copilot SDK observed FWS transport

- Fact: `copilot-sdk` now runs the Copilot CLI through framework-shells raw pipe mode (`backend: pipe`, `pipe.mode: native_pipe_testing`) and injects an external process-like transport into the vendored SDK instead of relying only on SDK-managed `subprocess.Popen`.
- Why it matters: Copilot stdio now stays observable through framework-shells JSON-RPC logs and can use framework-shell-owned input/output without wrapping the shim under `node` or racing direct pipe readers.
- Citations: `extensions/copilot_sdk/shellspec/copilot_cli.yaml`; `extensions/copilot_sdk/fws_pipe_process.py`; `extensions/copilot_sdk/client.py`; `extensions/copilot_sdk/_vendor/copilot/client.py`; `extensions/copilot_sdk/_vendor/copilot/types.py`

### Copilot native transcript IDs

- Fact: Copilot raw events expose stable `messageId` / `reasoningId`, while `assistant.turn_start.data.turnId` resets after `session.resume`; replay-safe transcript IDs must not rely on router-local `msg_<turn>_<block>` counters.
- Why it matters: router recreation after resume/reattach can reuse local turn/message counters inside the same conversation, and the harness transcript dedupe key `(conversation_id, item_id, role)` can silently drop later assistant rows even though live emit and preview still succeed. Future Copilot replay/parity work should anchor assistant/reasoning IDs to provider-native IDs and treat turn IDs as resume-scoped unless paired with another stable signal.
- Citations: `extensions/copilot_sdk/router.py:425-465,722-795,1651-1671`; `extensions/copilot_sdk/client.py:2784-2825`; `/data/data/com.termux/files/home/.cache/framework_shells/runtimes/b4f2d44683b344a8/0a37fcbc242ec454/logs/fs_1776047944_24bb44cd.stdout.log:17,65,393,467,521,23529,23581,24871,24933`

### Copilot lazy resume deterministic resend contract

- Fact: Copilot lazy resume should not reapply `session.mode.set` on unchanged bound-session sends, and cold `session.send` misses now recover through a deferred backend resend: the first `Session not found` is only the trigger, `session.resume` success is the reattach ack, and the retry send happens after that resume succeeds. The frontend `conversation.send` timeout stays at the generic 10s because the resend no longer depends on stretching the RPC window.
- Why it matters: the intended contract is `send -> resume -> retry send`, not `resume -> mode.set failure -> no retry` and not “wait longer and hope”. The mode cache prevents resumed sessions from getting stuck on `session.mode.set`, and the deferred resend keeps the frontend generic while still letting Copilot recover deterministically after a cold miss.
- Citations: `extensions/copilot_sdk/client.py`; `agent_log_server/static/js/codex_agent/rpc/conversations/client.ts`; `/data/data/com.termux/files/home/.cache/framework_shells/runtimes/b4f2d44683b344a8/0a37fcbc242ec454/logs/fs_1776473012_9063d5ea.stdout.log`; `/data/data/com.termux/files/home/.cache/framework_shells/runtimes/b4f2d44683b344a8/0a37fcbc242ec454/logs/fs_1776475855_9f0d28d1.stdout.log`

### Generic lazy-resume hook direction

- Fact: The reusable abstraction is not “resume on conversation enter”; it is an extension-owned lazy-resume state machine: transcript-first conversation load stays cold, the first live action probes the provider, cold-miss triggers provider-specific resume, resume success is the ack boundary, then the original action retries once.
- Why it matters: this preserves the current efficient session lifecycle while still allowing a future generic extension contract around `cold_miss -> resume -> ack -> retry` instead of duplicating that logic ad hoc per provider.
- Citations: `AGENT_EXTENSION_INTEGRATION.md`; `CODEX_APP_SERVER_EXTENSION.md`; `extensions/copilot_sdk/client.py`; `extensions/codex_ext/client.py`; `worktrees/gemini_acp_extension/transport.py`


## Extension packaging



### codex-ext-testing shim removal
- Fact: `codex-ext-testing` was a disabled compatibility shim pointing at the real `codex_ext` package and has been removed from `extensions/extensions.json`; it should not be treated or restored as a real extension.
- Why it matters: schema/settings modal iteration treats registry entries as requestable extensions. Reintroducing a fake registry entry can send adapter RPCs to a non-real extension id and destabilize the long-lived Python adapter path.
- Citations: `extensions/extensions.json`; `User input: codex-ext-testing is a shim, It shouldn't actually request anything, Just get rid of that extension.`
### Package install vs runtime activation

- Fact: Extension package install/update is separate from runtime reload and optional dependency install/readiness; callers perform install/update first, then explicitly reload runtime state and optionally run dependency install.
- Why it matters: future installer, reload, extension-management, and documentation work must not assume that package install alone activates an extension in the live runtime. The CLI and HTTP admin surfaces both follow the same split, which affects availability state, readiness waits, and user-facing management flows.
- Citations: `agent_log_server/extension_cli.py:138-155,169-180,208-216`; `agent_log_server/extension_api.py:511-557,559-607,631-656`; `extensions/__init__.py:1076-1186`
### Manifest version invariant

- Fact: Every builtin and user-installed extension `manifest.json` must include a non-empty `version` string; if a legacy manifest is being versioned for the first time, start at `0.1.0`.
- Why it matters: Manifest version is part of the install/runtime contract for reporting and upgrade logic, and runtime discovery must not silently recover from a missing manifest version by falling back to registry or installer metadata.
- Citations: `User input: This is an INVARIANT. Every extension manifest must be versioned from this point forward. If it doesn't have a version, make one. Start from 0.1.0`; `EXTENSIONS_CONTRACT.md`; `extensions/__init__.py`; `AGENT_EXTENSION_INTEGRATION.md`


### Load-session quiet-period barrier

- Fact: If an extension uses a history-bearing `load_session()` as the synthetic reattach ack for a cold-session retry path, replay suppression must stay active until replay notifications reach a real quiet period; RPC completion alone is not enough.
- Why it matters: delayed replay notifications can continue arriving after the `load_session()` result is delivered. If the transport switches into live prompt capture too early, old history can be absorbed into the retried live turn and pollute the assistant transcript row.
- Citations: `AGENT_EXTENSION_INTEGRATION.md`; `CODEX_APP_SERVER_EXTENSION.md`; `worktrees/gemini_acp_extension/README.md`; `User input: Anyway, it failed again. Same transcript, same issue. This time, instead, the rehydration came in as a single event/message.`

### Frontend harness source entrypoint

- Fact: The ALS-RS frontend harness source entrypoint is `rust/crates/als-server/src/static/codex_agent.ts`; `build.mjs` bundles that file to `rust/crates/als-server/src/static/dist/codex_agent.js`.
- Why it matters: future ALS-RS frontend work should target the Rust-owned static tree and bundle pipeline, not the deprecated Python package path. This keeps source edits, packaging, and Rust static serving aligned with the active app shell.
- Citations: `build.mjs:6-7,122-126`; `rust/crates/als-server/src/config.rs:216-224`; `agent_log_server_rs/bootstrap.py:126,360-365`

### Deprecated Python package

- Fact: The legacy Python server package has been renamed to `als_deprecated`; active ALS-RS HTML/static serving lives under `rust/crates/als-server/src/`, while Python remains bootstrap/adapter/deprecated-reference code.
- Why it matters: future ALS-RS runtime changes should not target `als_deprecated` server/page code as the active app path. Shared helper imports may still point at `als_deprecated`, but browser-facing static/template work belongs in the Rust server tree.
- Citations: `pyproject.toml:13-25`; `server.py:1-6`; `rust/crates/als-server/src/index.html`; `rust/crates/als-server/src/static/`

### Root frontend decomposition slices

- Fact: `rust/crates/als-server/src/static/codex_agent.ts` delegates composer/draft behavior to `composer/runtime.ts`, widescreen layout to `layout/widescreen.ts`, host UI/app-config helpers to `host/runtime.ts`, row/counter helpers to `timeline/rows.ts`, live assistant/reasoning/diff/approval/agent-block rendering to `timeline/live_items.ts`, and replay/reset logic to `timeline/replay.ts`.
- Why it matters: future frontend work should target the extracted module that owns the behavior instead of rebuilding large helper walls in the root entrypoint. This keeps the remaining `codex_agent.ts` cuts focused on the next ownership seams (`plan/runtime`, `conversation/runtime`, `render/utils`, and `subagents/collapsible`) rather than regressing the already-completed host/timeline slices.
- Citations: `rust/crates/als-server/src/static/codex_agent.ts`; `rust/crates/als-server/src/static/js/codex_agent/composer/runtime.ts`; `rust/crates/als-server/src/static/js/codex_agent/layout/widescreen.ts`; `rust/crates/als-server/src/static/js/codex_agent/host/runtime.ts`; `rust/crates/als-server/src/static/js/codex_agent/timeline/rows.ts`; `rust/crates/als-server/src/static/js/codex_agent/timeline/live_items.ts`; `rust/crates/als-server/src/static/js/codex_agent/timeline/replay.ts`

- Fact: `rust/crates/als-server/src/static/codex_agent.ts` also delegates render/text helpers to `render/utils.ts`, subagent/collapsible behavior to `subagents/collapsible.ts`, plan overlay/modal state to `plan/runtime.ts`, and conversation/socket runtime flows to `conversation/runtime.ts`.
- Why it matters: the root entrypoint is now mostly orchestration glue, so future frontend cleanup should target remaining seams instead of reintroducing plan, conversation, or render helper walls into `codex_agent.ts`. This also marks the decomposition frontier before the planned strict-mode pass over TypeScript and Pyright settings.
- Citations: `rust/crates/als-server/src/static/codex_agent.ts`; `rust/crates/als-server/src/static/js/codex_agent/render/utils.ts`; `rust/crates/als-server/src/static/js/codex_agent/subagents/collapsible.ts`; `rust/crates/als-server/src/static/js/codex_agent/plan/runtime.ts`; `rust/crates/als-server/src/static/js/codex_agent/conversation/runtime.ts`

### Copilot protocol adapter boundary
- Fact: `copilot-sdk` now isolates vendored SDK drift behind `extensions/copilot_sdk/protocol_adapter.py`; client-side resume/compaction wrappers and router event access go through `CopilotEventView` instead of reaching directly into the generated `SessionEventData` union.
- Why it matters: future Copilot SDK updates should be absorbed at the adapter boundary first, keeping `client.py` and `router.py` off raw generated payload classes and making basedpyright meaningful again.
- Citations: `extensions/copilot_sdk/protocol_adapter.py`; `extensions/copilot_sdk/client.py`; `extensions/copilot_sdk/router.py`

### Copilot schema vs generated RPC drift registry
- Fact: `extensions/copilot_sdk/protocol_registry.py` vendors and loads the official Copilot `api.schema.json` and `session-events.schema.json`, but it also scans the generated Python RPC surface because raw schema and generated/public SDK can disagree. Current concrete drift: the vendored schema still advertises `session.compaction.compact`, while the generated SDK/public surface exposes `session.history.compact`.
- Why it matters: for Copilot, raw schema alone is not a safe runtime authority. Future protocol work should reconcile schema + generated SDK surface, and runtime call sites should prefer the generated/public API surface with schema used as validation and drift detection.
- Citations: `extensions/copilot_sdk/protocol_registry.py`; `extensions/copilot_sdk/schemas/api.schema.json`; `extensions/copilot_sdk/_vendor/copilot/generated/rpc.py`; `worktrees/copilot-sdk/python/e2e/test_agent_and_compact_rpc.py`

### Copilot reasoning effort metadata preservation
- Fact: Copilot GPT-family `xhigh` can exist only in raw `capabilities.supports.reasoning_effort` even when the top-level `supportedReasoningEfforts` array is truncated. The vendored Copilot `ModelInfo.from_dict()` now merges those sources so `extensions/copilot_sdk/client.py:list_models()` publishes the full effort set.
- Why it matters: settings UIs must consume actual model-supported efforts from extension model metadata; otherwise GPT/Codex models lose `xhigh` before the modal ever sees it.
- Citations: `extensions/copilot_sdk/_vendor/copilot/client.py`; `extensions/copilot_sdk/client.py`; `/data/data/com.termux/files/home/.cache/framework_shells/runtimes/b4f2d44683b344a8/0a37fcbc242ec454/logs/fs_1776561526_c8b8f60f.stdout.log`

### Settings effort options must not invent fallbacks
- Fact: both settings UI paths now derive reasoning-effort choices only from per-model metadata (including nested capability lists) and no longer invent a generic `low/medium/high` fallback when a model omits effort support.
- Why it matters: the modal and main settings UI need to reflect real provider capabilities consistently across Copilot and Codex instead of masking metadata drift with fake options.
- Citations: `agent_log_server/static/modals/settings_schema.js`; `agent_log_server/static/js/codex_agent/settings/ui_flow.ts`

### Copilot compaction should not force a client-side timeout
- Fact: Copilot compaction now calls the SDK compact API without the fixed `30s` timeout from the extension layer.
- Why it matters: the runtime can emit a successful compaction sequence after 30 seconds; extension-owned timeout enforcement caused false error reporting even when compaction succeeded.
- Citations: `extensions/copilot_sdk/client.py`; `/data/data/com.termux/files/home/.cache/framework_shells/runtimes/b4f2d44683b344a8/0a37fcbc242ec454/logs/fs_1776561526_c8b8f60f.stdout.log`

### Codex ask-user timeout is extension-injected
- Fact: `codex-ext` and `codex-ext-exp` now inject `mcp_servers.agent-pty-blocks.tool_timeout_sec` with a 1-hour minimum (`3600`) in their thread-config builders, while preserving any higher existing value.
- Why it matters: the live Codex CLI config may still carry a shorter global MCP timeout, but extension-owned conversations now raise the `ask_user` ceiling per thread without mutating `~/.codex/config.toml`. Future timeout work should treat the runtime-protocol MCP server injection as the repo-owned control point for Codex ask-user duration.
- Citations: `extensions/codex_ext/runtime_protocol.py`; `extensions/codex_ext_exp/runtime_protocol.py`; `/data/data/com.termux/files/home/.codex/config.toml`

### codex-ext-exp runtime protocol uses a vendored schema bundle
- Fact: `codex-ext-exp` now loads its runtime schema from the bundled snapshot under `extensions/codex_ext_exp/schemas/0.118.0/` instead of `~/.cache/app_server/codex_app_server_schema/...`, and packaging includes that schema directory.
- Why it matters: the experimental Codex extension is now deterministic and self-contained at startup, without depending on a machine-local cache or runtime schema generation. Future schema refresh work should update the vendored snapshot and package-data rules rather than restoring cache-root discovery for `codex-ext-exp`.
- Citations: `extensions/codex_ext_exp/runtime_protocol.py`; `extensions/codex_ext_exp/schemas/0.118.0/codex_app_server_protocol.v2.schemas.json`; `pyproject.toml`; `MANIFEST.in`

### Copilot protocol adapter must stay thin
- Fact: `extensions/copilot_sdk/protocol_adapter.py` should stay thin: `resume_sdk_session()` forwards supported `resume_session()` kwargs by signature, and `CopilotEventView` must be a zero-copy view over raw SDK event data instead of normalizing the whole payload per event.
- Why it matters: broad payload normalization in the adapter added hot-path overhead to every Copilot event, and hand-copied resume kwargs dropped `streaming` / `include_sub_agent_streaming_events`, which broke assistant deltas on resumed sessions. Future Copilot SDK updates should preserve the signature-filtering resume seam and keep the event adapter transparent rather than rebuilding a second payload model.
- Citations: `extensions/copilot_sdk/protocol_adapter.py`; `extensions/copilot_sdk/client.py`

## Workflow and contribution policy


### ALS-RS migration plan upkeep

- Fact: Keep `worktrees/notes/ALS_RS_MIGRATION_PLAN.md` current after each ALS-RS migration phase lands; it is also included in the active KB file list as the primary ALS-RS migration planning anchor.
- Why it matters: The ALS-RS branch is moving quickly across Rust runtime, adapter, registry, schema, and lifecycle slices; this note is the durable migration map and should not lag behind implemented phases or be hidden behind deprecated KB docs.
- Citations: `User input: "Ok lets do that, and remember to keep it updated, then"`; `worktrees/notes/ALS_RS_MIGRATION_PLAN.md`; `.agent-pty.toml`.
### Commit authorship is user-controlled
- Fact: In this repo, Copilot attribution on commits is opt-in only: do not add `Copilot` as an author/co-author or add a `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` trailer unless the user explicitly requests that exact attribution for the specific commit.
- Why it matters: commit authorship is treated here as a repo-local ownership and consent boundary, not a default platform behavior. Future agents should ask when attribution is unclear and should not mistake session/platform Copilot trailer defaults for this repo's policy.
- Citations: `User input: You are no longer allowed to commit as "copilot" from this point forward.`; `User input: That copilot models are not allowed to commit With a co-author status. EVER.`; `User input: As a matter of fact, write that into the developer instructions template.`

### Codex per-turn MCP injection
- Fact: Codex MCP config should be injected on every `turn/start` as well as thread start/resume. The injected `agent-pty-blocks` server sends the full `enabled_tools` list parsed from `mcp_agent_pty_server.py` and intentionally omits `disabled_tools`; static disabled filters should not be used for this harness injection path.
- Why it matters: Codex recursively merges `params.config` with static/project config, so per-turn injection must carry the full PTY-blocks tool allowlist to avoid tool-list collapse. Omitting harness `disabled_tools` matches the current desired contract while the full allowlist keeps the injected tool set authoritative.
- Citations: `extensions/codex_ext/runtime_protocol.py:1360-1372`; `extensions/codex_ext/runtime_protocol.py:1469-1492`; `extensions/codex_ext_exp/runtime_protocol.py:1248-1261`; `extensions/codex_ext_exp/runtime_protocol.py:1353-1366`; `/data/data/com.termux/files/home/downloads/codex/codex-rs/config/src/merge.rs:3-17`

### Dynamic-only harness MCP config
- Fact: The splash/global TE2 MCP static Codex config writer has been removed. Harness MCP config is dynamic via per-conversation/per-turn injection, and the active `~/.codex/config.toml` should not define top-level `mcp_servers` for `agent-pty-blocks` or `te2-mcp` for harness use.
- Why it matters: Static Codex MCP config can merge with injected config and cause random/partial tool inventories. Future debugging should treat dynamic injection as authoritative and avoid reintroducing the splash/global static writer.
- Citations: `agent_log_server/web_pages.py`; `agent_log_server/static/js/codex_agent/host/runtime.ts`; `agent_log_server/appserver_routes.py`; `agent_log_server/te2_sync.py`

### ALS-RS provider binding parity

- Fact: ALS-RS conversation create/update strips schema `settings.session`, normalizes it into `meta.thread_id` / `meta.provider_session_id`, calls the generic adapter `conversation.resume` hook for extension-owned `resume_session_with_history()` + `hydrate_transcript()`, and sends existing bindings back to the adapter on `conversation.send` for lazy resume.
- Why it matters: restored Codex rollouts and Copilot sessions must be bound to the harness conversation immediately, while existing bound conversations must survive adapter restart/reload without losing the provider thread/session ID.
- Citations: `rust/crates/als-server/src/conversation_rpc.rs`; `agent_log_server_rs/adapters/extension_adapter.py`; `rust/crates/als-adapter-protocol/src/lib.rs`; `agent_log_server_rs/adapter_protocol.py`
### ALS-RS generic MCP contract

- Fact: ALS-RS now passes a generic `mcp_context` DTO on adapter `conversation.start`, `conversation.resume`, and `conversation.send`. Rust owns the provider-neutral context (`conversation_id`, `cwd`, requested `mcp_servers`, always-on stdio `agent-pty-blocks`, optional `te2-mcp` intent), the Python adapter forwards it as `settings.mcp_context`, and extension-local `mcp_contract.py` modules translate it into provider-native MCP config.
- Why it matters: MCP shaping is now extension-owned without pushing provider-specific config logic into ALS-RS. Future MCP work should extend the generic DTO or the extension contracts instead of teaching the Rust server how each provider wants `mcp_servers` laid out.
- Citations: `rust/crates/als-adapter-protocol/src/lib.rs`; `rust/crates/als-server/src/conversation_rpc.rs`; `agent_log_server_rs/adapters/extension_adapter.py`; `extensions/copilot_sdk/mcp_contract.py`; `extensions/codex_ext/mcp_contract.py`

### ALS-RS footer runtime options

- Fact: ALS-RS `/rpc/settings` `extension.runtimeOptions.get` resolves conversation metadata/settings in Rust, then calls adapter `extension.get_runtime_options`, which delegates to the extension loader's schema/provider runtime-option merger.
- Why it matters: footer controls are a settings/runtime-options surface, not provider-specific conversation logic. Future footer work should extend the extension schema/runtime-options contract and adapter method instead of hardcoding quick controls in Rust or the frontend.
- Citations: `rust/crates/als-server/src/settings_rpc.rs`; `rust/crates/als-adapter-protocol/src/lib.rs`; `agent_log_server_rs/adapters/extension_adapter.py`; `extensions/__init__.py`

### ALS-RS direct and proxied frontend sockets

- Fact: ALS-RS frontend RPC namespaces must work from both the direct server origin and the TE2 proxy mount. Direct pages use ALS-RS `/socket.io` for `/rpc/*` namespaces and connect the TE2 console bridge to the TE2 origin, while proxied pages keep proxy-relative Socket.IO paths.
- Why it matters: direct-port introspection should not depend on TE2 proxy URL rewriting, and proxied behavior should not require a separate code path for settings/schema provider RPCs. Future frontend route work should keep schema dynamic sources on `/rpc/settings` and avoid waiting on unrelated conversation live sockets before provider option loads.
- Citations: `agent_log_server/static/js/codex_agent/rpc/transport.ts`; `agent_log_server/static/modals/settings_schema.ts`; `agent_log_server/static/js/console_bridge.js`; `rust/crates/als-server/src/index.html`

### ALS-RS TE2 semantic readiness

- Fact: ALS-RS posts minimal TE2 semantic readiness from Rust server startup after the router is built, the TCP listener is bound, and `axum::serve` is spawned, sending only `{"status":"ready"}` to `/api/apps/{TE_APP_ID}/readiness`.
- Fact: This readiness signal is app-level only and deliberately does not send stateful `host_id`, `token_id`, `url`, or console-worker metadata.
- Why it matters: TE2 shellspec TCP readiness only proves the process/port is reachable, while TE2 may wait for app/backend readiness before requesting the app page. The semantic POST must therefore be emitted from backend lifecycle readiness, not from the index route, while stateful sidebar/window checkpointing remains a separate explicit contract.
- Citations: `rust/crates/als-server/src/main.rs`; `rust/crates/als-server/src/te2_readiness.rs`; agent-log messages 95, 98.

### ALS-RS agent log parity

- Fact: ALS-RS owns the agent-log surface under `${ALS_RS_CACHE_DIR}/agent-log/agent_chat.log.jsonl`, exposing Python-parity `/api/messages*`, `/ws`, `/appserver` `get_log_messages` / `post_log_message` / `agent_log_message`, and the `/agent-log` browser page.
- Why it matters: `agent-pty-blocks` agent-log MCP tools now follow `AGENT_LOG_SERVER_ORIGIN`, so ALS-RS-hosted conversations use the Rust port while non-ALS launches keep the legacy Python default. Future agent-log work should extend `rust/crates/als-server/src/agent_log.rs` and the Rust-served page shell instead of routing back to the Python server.
- Citations: `rust/crates/als-server/src/agent_log.rs`; `rust/crates/als-server/src/static_assets.rs`; `rust/crates/als-server/src/socketio.rs`; `agent_log_server/static/app.js`; `mcp_agent_pty_server.py`

### ALS-RS TE2 sidebar IPC

- Fact: ALS-RS uses a Rust Socket.IO client for TE2's `/sidebar_ipc` logical namespace, but now speaks the typed JSON-RPC contract over `rpc` / `rpc.notify`: `hostUi.recheck` calls `sidebar.cwd.get`, `/rpc/ui` `file.open` calls `sidebar.file.open`, and incoming `rpc.notify` handles `sidebar.cwd.set` / `sidebar.mention`. Narrow legacy `sidebar:*` fallback remains only for older TE2 instances during cutover.
- Why it matters: TE2 can retire the legacy sidebar event names while ALS-RS keeps the same frontend behavior and avoids HTTP fallbacks. Future sidebar work should extend `rust/crates/als-server/src/sidebar_ipc.rs` and `ui_rpc.rs` around the typed RPC contract, not reintroduce provider-specific or legacy-only paths.
- Citations: `rust/crates/als-server/src/sidebar_ipc.rs`; `rust/crates/als-server/src/ui_rpc.rs`; `SIDEBAR_IPC_RPC_CONTRACT.md`; `SIDEBAR_IPC_RPC_SCHEMA.md`
### ALS-RS app UI state

- Fact: ALS-RS persists minimal app UI state in `${ALS_RS_CACHE_DIR}/app_state.json` with `active_conversation`, `active_view`, `user_name`, and `show_console_worker_id`; boot-time `conversation.get` without an explicit ID hydrates that persisted active conversation without creating missing metadata.
- Why it matters: active conversation restore, splash username, and debug splash-display preferences are Rust-owned app state now, not legacy Python `app_server_config.json` state. Future restore/settings work should update `UiSelectionStore` and the RPC state surfaces instead of reviving the legacy config file.
- Citations: `rust/crates/als-server/src/state.rs`; `rust/crates/als-server/src/conversation_rpc.rs`; `rust/crates/als-server/src/settings_rpc.rs`; `rust/crates/als-server/src/index.html`; `agent_log_server/static/js/codex_agent/host/runtime.ts`

### ALS-RS Codex schema cache root

- Fact: When launched under ALS-RS, `codex-ext` schema cache discovery uses `${ALS_RS_CACHE_DIR}/codex_app_server_schema`; the legacy `~/.cache/app_server/codex_app_server_schema` path remains the fallback for non-ALS-RS launches.
- Why it matters: ALS-RS keeps its runtime/cache roots isolated from the Python harness while preserving backwards compatibility for the same extension running outside Rust.
- Citations: `rust/crates/als-server/src/adapter_process.rs`; `extensions/codex_ext/runtime_protocol.py`

### TE2-gated repo memory prompt context

- Fact: ALS-RS owns repo-memory/developer-instruction composition at adapter request boundaries through the `devins_context` DTO. Rust builds the effective string for conversation start/resume/send from raw `developer_instructions`, TE2 template state, and a fresh `.repo_memory.md` snapshot discovered from the effective cwd; the Python adapter injects it as `settings.__als_devins_context__`, and Copilot/Codex `devins_contract.py` modules consume that server-provided `effective` value.
- Why it matters: repo-memory/dev-instruction updates remain next-turn/resume/compact-boundary behavior, not live mid-turn push behavior, but extension code no longer reads `.repo_memory.md` or imports deprecated Python prompt-context helpers. Future ALS-RS memory work should keep concatenation server-owned and should not revive the deprecated `codex-ext-exp` pending-context path.
- Citations: `rust/crates/als-server/src/devins_context.rs`; `rust/crates/als-adapter-protocol/src/lib.rs`; `agent_log_server_rs/adapters/extension_adapter.py`; `extensions/copilot_sdk/devins_contract.py`; `extensions/codex_ext/devins_contract.py`; `worktrees/notes/ALS_RS_MIGRATION_PLAN.md`
### ALS-RS ask_user IPC bridge

- Fact: ALS-RS owns the `/ipc` Socket.IO namespace used by `mcp_agent_pty_server.py` when running under Rust. The stdio MCP server is the Socket.IO client, authenticates with `~/.cache/app_server/ipc_secret`, receives `ask_user_response`, sends `ask_user_ack`, and uses `CONVERSATION_ID` as the request id. ALS-RS injects `AGENT_LOG_SERVER_ORIGIN` through the generic MCP context so the MCP client connects to the Rust port.
- Why it matters: `agent-pty-blocks.ask_user` is not a normal provider approval. `conversation.approval.respond` must special-case `request_method == "agent-pty/ask-user"` by emitting the submitted response over `/ipc` and waiting for MCP ack before appending/removing the approval handoff; normal provider approvals still route through adapter `approval.respond`.
- Citations: `rust/crates/als-server/src/ipc.rs`; `rust/crates/als-server/src/conversation_rpc.rs`; `mcp_agent_pty_server.py`; `extensions/codex_ext/mcp_contract.py`; `extensions/copilot_sdk/mcp_contract.py`
### ALS-RS ferrous_framework transport
- Fact: ALS-RS has an optional `ferrous_framework` Rust crate for a PyO3-backed framework-shell native-pipe extension-adapter transport; default builds keep the direct child stdin/stdout fallback, while source-checkout bootstrap launches with `--features ferrous-framework-pyo3` when framework-shell context is present. The adapter shellspec is `agent_log_server_rs/shellspec/extension_adapter.yaml`.
- Why it matters: adapter observability can move into framework-shell-owned native pipes without clobbering Rust CLI stdout or adding a UDS/TCP broker hop. In this Termux environment, optional PyO3 feature validation must target the non-`t` Python 3.13 interpreter because ambient `PYO3_CONFIG_FILE` can point at the separate Python 3.14t config.
- Citations: `rust/crates/ferrous_framework/src/lib.rs`; `agent_log_server_rs/ferrous_framework.py`; `agent_log_server_rs/shellspec/extension_adapter.yaml`; `agent_log_server_rs/bootstrap.py`; `rust/crates/als-server/src/adapter_process.rs`; `worktrees/notes/ALS_RS_MIGRATION_PLAN.md`.
- Fact: The Copilot SDK FWS pipe wrapper can fail writes while the Copilot child process and both ends of the stdin pipe still exist; the preexisting generic `copilot sdk pipe write failed` message hides the inner exception. The diagnostic path logs the writer-task exception chain and pipe state detail from `extensions/copilot_sdk/fws_pipe_process.py`, while framework-shells should keep local live-pipe writes on the lightweight `load_shell_record(...)` path instead of `get_shell(...)` adoption refresh.
- Why it matters: repeated framework-shell adoption after lifecycle events is not proof of the Copilot write failure. Future debugging should inspect the logged inner exception before changing lifecycle/adoption semantics.
- Citations: `extensions/copilot_sdk/fws_pipe_process.py`; `framework_shells/manager.py`; live FWS shells `fs_1778491116_72289b4a` and `fs_1778491125_34d014bf` observed on 2026-05-11.
### ALS-RS bootstrap framework-shell secret
- Fact: ALS-RS bootstrap preserves inherited `FRAMEWORK_SHELLS_*` values, but standalone launches now seed a stable directory-fingerprinted framework-shell secret under `${XDG_CACHE_HOME:-~/.cache}/framework_shells/runtimes/<fingerprint>/secret` before starting Rust.
- Why it matters: packaged/standalone ALS-RS can initialize Python extension adapter framework-shell integrations without requiring a manual dummy `FRAMEWORK_SHELLS_SECRET`, while TE2/FWS-hosted launches keep their existing control-plane secret.
- Citations: `agent_log_server_rs/bootstrap.py`; `framework_shells/auth.py`; `mcp_agent_pty_server.py`
### ALS-RS cwd picker backend
- Fact: ALS-RS `/rpc/ui` now implements `filesystem.list` for cwd picker directory browsing and `filesystem.search` for mention search, returning picker-compatible `{ path, parent, items }` / `{ root, items }` payloads without frontend changes.
- Why it matters: the settings cwd picker and composer mention search no longer depend on Python `/api/fs/*` routes when running against ALS-RS; future UI RPC work should extend `rust/crates/als-server/src/ui_rpc.rs` rather than adding HTTP fallbacks.
- Citations: `rust/crates/als-server/src/ui_rpc.rs`; `agent_log_server/static/js/codex_agent/rpc/ui/client.ts`; `agent_log_server/static/js/codex_agent/settings/ui_flow.ts`; `worktrees/notes/ALS_RS_MIGRATION_PLAN.md`.

### Schema-declared dependent options
- Fact: Schema settings dependent selects use `dynamic_options_from` to declare their source field, model/source match path, option path, option value/label paths, and default path; the UI should not hardcode provider model metadata shapes.
- Why it matters: Codex generated schemas and Copilot static schemas can point at their own extension-facing model-list response contracts while sharing one renderer. Future schema UI work should extend the schema contract instead of adding provider-specific frontend normalization.
- Citations: `agent_log_server/static/modals/settings_schema.ts`; `extensions/codex_ext/runtime_protocol.py`; `extensions/codex_ext_exp/runtime_protocol.py`; `extensions/copilot_sdk/settings_schema.json`.

### ALS-RS approval contract

- Fact: ALS-RS approvals follow the legacy Python meta-file flow: live generic `type: "approval"` events persist `meta.pending_approvals`, `conversation.approval.respond` routes to adapter `approval.respond`, successful resolution appends a `role: "approval"` handoff transcript row, removes the pending descriptor, and emits `conversation.approval.handoff`.
- Why it matters: approvals/ask-user replay and reload fidelity depends on `pending_approvals` being the server-owned restore source and on handoff rows matching the existing frontend replay contract. Future approval work should extend `APPROVAL_CONTRACT.md` and the ALS-RS adapter boundary instead of inventing provider-specific frontend or Rust paths.
- Citations: `APPROVAL_CONTRACT.md`; `rust/crates/als-server/src/conversation_rpc.rs`; `rust/crates/als-server/src/conversation_store.rs`; `agent_log_server_rs/adapters/extension_adapter.py`; `agent_log_server/appserver_routes.py`; `agent_log_server/server.py`

### ALS-RS approval request-card assets

- Fact: ALS-RS serves extension-local custom approval/request-card assets from `/api/extensions/{extension_id}/assets/{asset_path}`, restricted to manifest-local `ui/...` and `static/...` paths. `extension.requestCards.get` returns module URLs for manifest `ui.requestCards`, so Copilot `copilot/user_input/request` and Codex `item/tool/requestUserInput` cards can load their custom renderers through the same generic approval lane.
- Why it matters: provider-native built-in ask-user prompts and custom approval cards are not a separate frontend transport. Future approval work should keep request-card discovery schema/manifest-driven and serve extension assets generically instead of adding provider-specific frontend bundles or hardcoded card imports.
- Citations: `APPROVAL_CONTRACT.md`; `rust/crates/als-server/src/static_assets.rs`; `rust/crates/als-server/src/settings_rpc.rs`; `extensions/copilot_sdk/manifest.json`; `extensions/codex_ext/manifest.json`

### ALS-RS footer interrupt routing

- Fact: ALS-RS `conversation.interrupt` routes through `/rpc/conversations` to the generic adapter `conversation.interrupt` method, which delegates to extension loader `interrupt_session(extension_id, conversation_id)`.
- Why it matters: footer interrupt is provider-owned like runtime options: Rust resolves the selected extension from conversation metadata, but Codex and Copilot keep abort semantics in their extension clients. Future interrupt work should extend the adapter/loader contract instead of adding provider-specific Rust or frontend branches.
- Citations: `rust/crates/als-server/src/conversation_rpc.rs`; `rust/crates/als-adapter-protocol/src/lib.rs`; `agent_log_server_rs/adapters/extension_adapter.py`; `extensions/__init__.py`
### ALS-RS multi-root extension packages
- Fact: ALS-RS loads extensions from ordered roots: builtin `ALS_RS_EXTENSIONS_DIR` first and user-installed `${ALS_RS_DATA_DIR}/extensions` second. Later roots override duplicate extension IDs, registry entries carry `source_root` / `source_kind`, and extension assets are served from the entry-owned root.
- Why it matters: third-party installs must be isolated under ALS-RS data roots while preserving the legacy Python multi-root semantics. Future registry, asset, and settings work should use entry-owned roots instead of joining paths against the builtin root.
- Citations: `rust/crates/als-server/src/config.rs`; `rust/crates/als-server/src/extension_registry.rs`; `rust/crates/als-server/src/static_assets.rs`

- Fact: ALS-RS package lifecycle RPCs use `extension.package.validate`, `extension.package.install`, `extension.package.update`, and `extension.package.remove`; the older `extension.install` remains dependency-install for an already registered extension.
- Why it matters: package installation and dependency installation are separate contracts. Future UI/admin work should call the `extension.package.*` methods for third-party source mutation and keep provider dependency hooks on the existing dependency install path.
- Citations: `rust/crates/als-server/src/settings_rpc.rs`; `agent_log_server_rs/adapters/extension_adapter.py`; `agent_log_server_rs/adapter_protocol.py`

- Fact: `als-rs extension ...` is an offline-first file-I/O CLI for second-root package install/update/remove/validate/list/reload; it initializes the Python loader with builtin + `${ALS_RS_DATA_DIR}/extensions` roots, supports path/zip/git sources, and only best-effort notifies a running server to reload afterward.
- Why it matters: third-party extension installs can run without the ALS-RS server and can also run in parallel with it. Future CLI/admin work must not make local package mutation network-bound or bypass the user extension root.
- Citations: `agent_log_server_rs/extension_cli.py`; `agent_log_server_rs/bootstrap.py`; `extensions/__init__.py`
### ALS-RS packaged Rust workspace
- Fact: ALS-RS pip wheels package the Rust workspace under `agent_log_server_rs/rust/`; bootstrap prefers source-checkout `rust/Cargo.toml`, falls back to the packaged manifest, points `ALS_RS_EXTENSIONS_DIR` at packaged builtin `extensions/`, and sets `CARGO_TARGET_DIR` under the ALS-RS cache root for packaged launches.
- Why it matters: pip/git installs must not assume repo-root `rust/` or extension directories exist beside site-packages, and cargo build output should not be written into installed package directories. Future bootstrap/package work should preserve this source-checkout vs packaged fallback.
- Citations: `setup.py`; `MANIFEST.in`; `pyproject.toml`; `agent_log_server_rs/bootstrap.py`

### ALS-RS graceful shutdown
- Fact: ALS-RS handles SIGINT/SIGTERM with bounded graceful shutdown: adapter `extension.shutdown` runs first, then the adapter transport is closed or killed, then Axum graceful shutdown is notified with a bounded drain.
- Why it matters: extension bridge/provider child processes should not keep standalone ALS-RS alive after termination, and shutdown logic must avoid broad in-process framework-shell group kills that could terminate the Rust server before cleanup.
- Citations: `rust/crates/als-server/src/main.rs`; `rust/crates/als-server/src/adapter_process.rs`; `agent_log_server_rs/adapters/extension_adapter.py`; `rust/crates/ferrous_framework/src/lib.rs`


### Provider live-session request contract
- Fact: Provider in-memory session state/unload is opt-in through manifest capability `capabilities.live_session_request: true`; the shared settings UI calls generic RPC methods `extension.session.state.get` and `extension.session.unload`, and ALS-RS/Python gate provider calls on that manifest flag.
- Why it matters: loaded-state pills and kill switches must remain cold-safe and generic. Future extensions should advertise this capability before implementing live-session hooks, and unsupported or failed requests should degrade to neutral/unsupported UI without breaking settings.
- Citations: `extensions/codex_ext/manifest.json`; `extensions/copilot_sdk/manifest.json`; `rust/crates/als-server/src/settings_rpc.rs`; `agent_log_server/static/modals/settings_schema.ts`; `agent_log_server_rs/adapters/extension_adapter.py`
### KB schema/info/read contract
- Fact: KB discovery is target-file oriented: `kb_list()` enumerates configured markdown targets, `kb_schema(file="...")` or `kb_schema(target="...")` returns the complete compact numbered ATX heading index for one target file, `kb_info(...)` returns parent-chain context/ranges/child headings/body previews for selected sections, and `kb_read(...)` returns parent heading/body context plus selected target bodies. `max_depth` is only an explicit schema narrowing option.
- Why it matters: agents should not call shallow `kb_schema(max_depth=2)` as the normal discovery path or treat schema calls as body reads. The intended flow is schema for the full index, info for range/context judgment, then read for chosen sections.
- Citations: `mcp_agent_pty_server.py`; `KB_MEMORY_MCP.md`; `kb_tool_issues.md`; `.agent-pty.toml`
### ALS-RS settings schema ownership

- Fact: ALS-RS settings schemas are server file-read only: Rust reads each registered extension's `settings_schema.json` from its source root/path for `extension.settingsSchema.get` and does not call adapter/extension `get_settings_schema` fallback hooks. Missing schema files return an empty server-owned schema with `schema_source: "missing_file"`.
- Fact: Settings modal dynamic selects and footer quick controls share the same runtime-options descriptor contract. A schema field declares `semantic.role` and `semantic.runtime_key`; `extensions.get_runtime_options()` merges that schema entry with provider-owned runtime descriptors into `runtimeOptions[<runtime_key>]`, `runtimeOptions.fields[<field_id>]`, and schema-derived `quickControls`. Frontend controls must save through the descriptor `settingKey` and must not invent legacy setting-key fallbacks or hard-code `approval` / `mode` controls.
- Fact: Dynamic select fields preserve selected raw option metadata and support schema-declared multi-field `write_back.on_select` rules. The renderer copies provider-neutral schema paths, including fallback paths, from the selected option into visible or hidden ordinary settings fields, and dependent local option sets clear stale values or apply schema defaults when their source field changes.
- Fact: Schema write-back is non-destructive by default: unresolved, null, or empty-string source paths do not clear existing target fields unless the rule explicitly opts into clearing with `clear`, `allow_empty`, or `allow_null`. Same-value write-back does not dispatch dependency refresh events, and dependent selects are resolved through the generic `dynamic_options_from` contract rather than hardcoded field ids such as `reasoning_effort`.
- Fact: Provider status and usage in the schema settings modal are also schema-declared semantics. Schema `info` fields declare `semantic.role` values `provider_status` / `provider_usage` with `semantic.runtime_key` values `status` / `usage`; the modal calls `/rpc/settings` `extension.providerInfo.get`, Rust forwards adapter method `extension.get_provider_info`, and each extension returns one DTO with `status` and `usage` members.
- Fact: Codex `mode` options remain dynamic and are sourced from Codex app-server `collaborationMode/list` in the extension runtime-options path. Because the current Codex schema bundle defines `CollaborationModeMask` but omits a generated `CollaborationModeListResponse`, `runtime_protocol.py` carries a method-specific inline response schema for this experimental RPC.
- Fact: Schema modal provider-session info is scoped to the conversation's bound extension; switching the selected settings agent no longer makes provider usage/session controls point at the wrong extension.
- Fact: Settings schemas support provider-neutral dynamic submenu/group fields. `type: "submenu"` / `type: "group"` can contain inline `fields` or lazy-load `schema_ref.target` through `/rpc/settings` method `extension.settingsSchema.fragment.get`; targets are extension-root-relative files guarded against absolute paths, parent/root components, path prefixes, and symlink traversal. Fields also support generic `visible_if`, `enabled_if`, and `clear_when_hidden` conditions using field-id predicates and `all` / `any` / `not` compounds.
- Fact: Conditional schema rows must be removed from layout when hidden. The Rust static settings CSS scopes `#settings-extension-fields [hidden]` and `.is-hidden-by-condition` to `display: none !important` so schema labels, submenus, info rows, and interaction rows do not occupy space after `visible_if` hides them.
- Fact: Settings schemas support provider-neutral submit-style interaction fields. A `type: "interaction"` schema field declares transient inputs, RPC source/action, request param mapping tokens, output renderer (`list`, `info`, or `json`), and optional write-back rules; the frontend calls `/rpc/settings` method `extension.schemaInteraction.run`, Rust forwards adapter method `extension.schema_interaction.run`, and the extension handles provider-native lookup/search/config-write logic through `run_schema_interaction(...)`.
- Fact: Schema interaction fields treat explicit `inputs: []` as a button-only interaction with no input rows. Omitted `inputs` still defaults to one `query` input, and the legacy `input` object remains a single-input shortcut.
- Fact: Schema interaction submit results can return generic `actions` for `refresh_options`, `upsert_option`, `select_option`, submit-result `write_back` / `apply_write_back`, submenu/group `collapse` / `open`, `mark_dirty`, and `open_url`; ALS-RS follows schema field ids and JSON paths only. `open_url` routes through generic `/rpc/ui` `url.open`, which validates `http` / `https` URLs and launches them with `xdg-open`.
- Fact: Schema fields can opt out of persistence with `persist: false`, `transient: true`, `secret: true`, `sensitive: true`, or `type: "secret"`; those values remain available to the open modal and schema interactions but are sent as null settings patches so they do not remain in `meta.settings`.
- Fact: Settings schema dynamic sources can pass schema-declared `source_params` / `source.params` using the same `$field.*`, `$input.*`, `$context.*`, and `$values` token language as schema interactions. Model-list params now travel through `/rpc/settings` `extension.models.list`, Rust settings RPC, the Python adapter, and `extensions.list_models(...)`; extensions opt into params by named handler parameters or `**kwargs`, while no-argument model-list handlers remain valid.
- Fact: Provider/model hierarchy is schema-declared and provider-neutral. A field can declare semantic role `provider`, a model field can declare `source_params` such as `{ "provider": "$field.provider" }`, and `depends_on` / `refresh_on` controls gating and refresh behavior. ALS-RS forwards the declared values and does not interpret OpenCode, OpenRouter, Codex, Copilot, or other provider-native meanings.
- Fact: Settings schemas support a provider-neutral `type: "provider_auth"` launcher. The modal calls extension-owned `extension.schemaInteraction.run` actions for provider/method listing, auth start, status, and completion; it supports multiple labeled auth methods, transient provider-owned prompt fields, API-key style prompts, OAuth `open_url`, and existing success actions such as refresh/upsert/select/write-back/collapse/mark_dirty. Auth prompt values and secrets are transient and must not persist into conversation `meta.settings`.
- Fact: Schema selects with more than ten loaded options switch from inline dropdown rendering to a settings option picker with regex filtering over label/value/detail. Selecting from the picker uses the same select/write-back/dependency-refresh path as normal dropdown selection and has no filesystem parent-directory behavior.
- Why it matters: The schema file is the authoritative UI contract that translates user intent into extension settings, transient provider lookups, action-only provider saves, and read-only provider info, while extension Python stays focused on provider-native request translation, dynamic data sources, account/quota calls, runtime values, and interaction handlers. Future settings work should edit `settings_schema.json` plus generic semantic renderers instead of adding provider-specific modal branches, storing credentials in conversation metadata, or rendering confusing empty query fields for button-only actions.
- Citations: `rust/crates/als-server/src/settings_rpc.rs`; `rust/crates/als-server/src/static/codex_agent.css`; `rust/crates/als-server/src/static/modals/settings_schema.ts`; `rust/crates/als-server/src/static/js/codex_agent/rpc/settings/client.ts`; `rust/crates/als-server/src/static/js/codex_agent/rpc/settings/contract.ts`; `rust/crates/als-server/src/static/js/codex_agent/runtime_footer.ts`; `rust/crates/als-server/src/static/js/codex_agent/settings/save_flow.ts`; `agent_log_server_rs/adapters/extension_adapter.py`; `agent_log_server_rs/adapter_protocol.py`; `rust/crates/als-adapter-protocol/src/lib.rs`; `extensions/codex_ext/client.py`; `extensions/copilot_sdk/client.py`; `extensions/codex_ext/runtime_protocol.py`; `extensions/codex_ext/settings_schema.json`; `extensions/copilot_sdk/settings_schema.json`; `extensions/__init__.py`; `worktrees/notes/SCHEMA_SETTINGS_CONTRACT.md`; agent-log messages 99 and 100.

### ALS-RS adapter entrypoint ownership
- Fact: ALS-RS exposes only the generic `als-rs-extension-adapter` console script; the old provider-specific `als-rs-copilot-adapter` script and `agent_log_server_rs/adapters/copilot_sdk_adapter.py` wrapper were removed. Shared JSON-RPC adapter helpers now live in `agent_log_server_rs/adapters/rpc_common.py`.
- Fact: ALS-RS server/core adapter wiring is provider-neutral: Rust no longer exposes `/api/adapter/copilot/*`, no longer imports `adapter_routes`, no longer has `initialize_copilot`, and adapter config uses generic `python_bin`. The Python extension adapter requires an explicit `extension_id` instead of defaulting to Copilot.
- Why it matters: provider-specific adapter lanes should not be revived. Future provider work should flow through the extension registry/loader/adapter contract and keep provider-native behavior inside extension packages.
- Citations: `pyproject.toml`; `agent_log_server_rs/adapters/extension_adapter.py`; `agent_log_server_rs/adapters/rpc_common.py`; `rust/crates/als-server/src/http.rs`; `rust/crates/als-server/src/adapter_process.rs`; `rust/crates/als-server/src/config.rs`.

### ALS-RS project summary modal
- Fact: ALS-RS exposes project checkout summaries through generic `/rpc/ui` method `project.summary.get`; the Rust server computes git branch/head/status/diff stats with `git2` inside `spawn_blocking`, and the frontend Project modal opens files through the existing `file.open` TE2 deeplink path. Conversation-row hamburger menus can open the same Project modal scoped to that row's `settings.cwd`.
- Fact: Project modal changed-file entries are collapsible diff cards backed by the existing conversation diff renderer/highlight path. The backend returns per-file `diff_text` only under the 15 KiB `max_diff_bytes` cap, plus `diff_bytes` / `diff_truncated`; capped entries keep the summary header but disable diff rendering and line links. Diff line clicks preserve the caller-provided absolute file path when one is supplied.
- Fact: Conversation-scoped agent edit tracking is Rust-owned and process-local. When `settings.trackEdits` is true, ALS-RS captures live canonical `type: "diff"` adapter events into `agent_edits`, emits typed TE2 sidebar IPC `sidebar.file.edit`, exposes tracked diffs on `project.summary.get` as `agent_diffs`, and handles Project modal `project.agentDiff.accept` / `project.agentDiff.reject`; reject applies the reverse patch through the Rust UI RPC path and then clears the tracked diff.
- Fact: Project modal agent-edit refresh is event-based: Rust emits `/rpc/ui` notifications `project.agentDiff.added` after live diff capture and `project.agentDiff.removed` after accept/reject clears a tracked diff; the Project modal subscribes to those notifications and debounces `project.summary.get` refreshes while open, keeping tracked-diff DOM and git snapshot summary current without frontend-side git math.
- Fact: Project modal content is grouped into collapsible category sections for overview, agent edits, and changed-file status groups. Category collapsed state is persisted in browser localStorage by project root plus category, while individual diff-card expanded/collapsed state is persisted by absolute file path.
- Fact: Project modal git actions are generic `/rpc/ui` project methods: `project.git.stage`, `project.git.unstage`, `project.git.restore`, and `project.git.commit`. `project.summary.get` exposes staged/unstaged/untracked file counts plus per-file staged/unstaged/untracked flags; the modal uses those for file/category stage/restore controls, staged-file `Unstage` controls, and the overview commit dialog. Restore/reject-all actions require warning confirmation, and commit prompts to stage all when nothing is staged before showing the commit-message modal.
- Why it matters: project status, git mutation, and agent edit review are generic harness/worktree surfaces, not extension-specific features. Future project-modal, worktree-summary, git-action, or agent-edit work should extend the UI RPC DTO, Rust ledger, notification contract, and shared modal instead of adding Codex/Copilot-specific frontend or adapter lanes.
- Citations: `rust/crates/als-server/src/ui_rpc.rs`; `rust/crates/als-server/src/project_summary.rs`; `rust/crates/als-server/src/project_git.rs`; `rust/crates/als-server/src/agent_edits.rs`; `rust/crates/als-server/src/conversation_rpc.rs`; `rust/crates/als-server/src/sidebar_ipc.rs`; `rust/crates/als-server/src/static/js/codex_agent/project_modal.ts`; `rust/crates/als-server/src/static/js/codex_agent/rpc/ui/contract.ts`; `rust/crates/als-server/src/static/js/codex_agent/diff/rendering.ts`; `rust/crates/als-server/src/index.html`

### ALS-RS inline agent edit document state

- Fact: ALS-RS owns inline agent-edit review state as a minimal URI-scoped ledger in `inline_agent_edits`; TE2 must stay stateless for review data. TE2 requests `sidebar.agentEdits.documentState.get` on file open for the opened URI and renders only ALS-published `sidebar.agentEdits.publish` projections.
- Fact: File opening, edit review, and edit tracking are distinct contracts. `sidebar.file.open` is navigation only. `sidebar.agentEdits.*` is review-state hydration/publication/decision. `sidebar.file.edit` is only the optional tracking/navigation signal that may open the notification URI when tracking is enabled.
- Fact: Live tracked `type: "diff"` adapter events bridge into both generic systems: `conversation_rpc.rs` records the Project modal tracked diff, publishes a matching inline edit projection, sends TE2 `sidebar.agentEdits.publish`, then sends `sidebar.file.edit` as the optional tracking/navigation signal. ALS-RS registers its `/sidebar_ipc` client with `agentEdits` capability on connect.
- Fact: Inline editor accept/reject decisions must route through the same ALS-RS Project modal accept/reject implementation (`project.agentDiff.accept` / `project.agentDiff.reject`) before inline review state is cleared and republished. TE2 should not optimistically clear overlays for queued decisions; it waits for the authoritative ALS publish/clear notification.
- Fact: Project modal accept/reject removes the tracked diff, clears the matching inline ledger entry, sends TE2 `sidebar.agentEdits.clear`, then republishes the current document projection with `sidebar.agentEdits.publish` so any remaining widgets on the same URI are restored from ALS state. Project modal accept-all/reject-all loops the same per-diff RPCs, so bulk decisions use the same clear/republish path.
- Fact: Inline edit hunk DTOs are changed-block based rather than hunk-header-only: each hunk carries `kind`, `originalRange`, `modifiedRange`, `originalLines`, and `modifiedLines`; deletion and addition hunks still send the absent side as an empty line array so TE2 can render original/deleted-line zones deterministically.
- Fact: Project-modal and inline reject now use ALS-RS's internal programmatic reverse-patch applier instead of shelling out to `git apply --reverse`. The applier parses unified diffs, verifies the modified-side lines still exist with whitespace-tolerant matching, preserves actual context-line whitespace and file line endings, and refuses multi-file or mismatched hunks.
- Why it matters: inline editor affordances and Project modal review controls are two UI surfaces over the same ALS-owned accept/reject semantics, while TE2 remains an IDE peer that requests and renders document projections without caching them as source-of-truth state.
- Citations: `rust/crates/als-server/src/inline_agent_edits.rs`; `rust/crates/als-server/src/agent_edits.rs`; `rust/crates/als-server/src/reverse_patch.rs`; `rust/crates/als-server/src/conversation_rpc.rs`; `rust/crates/als-server/src/sidebar_ipc.rs`; `rust/crates/als-server/src/ui_rpc.rs`; `rust/crates/als-server/src/static/js/codex_agent/project_modal.ts`; `/data/data/com.termux/files/home/mrselect6/app/apps/file_editor_cm6/host/agent_edit_review_backend.py`; `/data/data/com.termux/files/home/mrselect6/app/apps/file_editor_cm6/monaco_editor/editor_agent_edit_review_runtime.ts`; `/data/data/com.termux/files/home/mrselect6/docs/planning/FILE_EDITOR_CM6_SIDEBAR_DRAFT_DISCOVERY_AND_AGENT_EDIT_REVIEW_PLAN.md`
### ALS-RS conversation fork contract

- Fact: ALS-RS exposes provider conversation branching through the generic `/rpc/conversations` method `conversation.fork` and adapter method `conversation.fork`. Rust allocates the target conversation id, asks the extension to fork the provider session into that id, then copies the local `transcript.jsonl` into the new conversation while rewriting row `conversation_id` and clearing draft/pending approval state.
- Fact: `codex-ext` advertises `capabilities.conversationFork` and implements the generic adapter method by calling Codex app-server `thread/fork` with `excludeTurns: true`; the Codex settings schema exposes the jump-off point as a generic `type: "action"` field with `semantic.role = "conversation_fork"`.
- Why it matters: conversation branching is a provider-neutral ALS contract. Future providers should implement the same adapter method/capability and schema action instead of adding Codex-specific frontend or server branches.
- Citations: `rust/crates/als-server/src/conversation_rpc.rs`; `rust/crates/als-server/src/conversation_store.rs`; `rust/crates/als-adapter-protocol/src/lib.rs`; `agent_log_server_rs/adapters/extension_adapter.py`; `extensions/codex_ext/client.py`; `extensions/codex_ext/settings_schema.json`; `rust/crates/als-server/src/static/modals/settings_schema.ts`.

### ALS-RS MCP draft safety preflight

- Fact: `agent-pty-blocks` exposes TE2 draft-discovery MCP tools through ALS-RS private `/ipc`: `sidebar_drafts_list`, `sidebar_draft_state_get`, `sidebar_draft_content_get`, and guarded `sidebar_draft_clear`. Rust allowlists and proxies only the implemented TE2 sidebar draft RPC methods (`sidebar.drafts.list`, `sidebar.draftState.get`, `sidebar.draft.clear`) through `sidebar_ipc`, so Rust remains the only TE2 `/sidebar_ipc` client.
- Fact: `agent-pty-blocks` requires `AGENT_LOG_SERVER_ORIGIN` for private IPC connection and must not silently fall back to the legacy `127.0.0.1:12359` origin for this path.
- Fact: Before editing existing files, agents should preflight TE2 user drafts: use `sidebar_drafts_list` for project/multi-file clean-state checks, `sidebar_draft_state_get(includeContent=false)` for targeted files, read content only with `sidebar_draft_content_get` when a relevant draft exists, and use `ask_user` if draft content appears meaningful before proceeding.
- Why it matters: TE2 drafts are user editor state and should not be silently clobbered by agent filesystem edits. The MCP tools provide a generic clobber-avoidance workflow without letting agents connect directly to TE2 sidebar IPC or reading draft content by default.
- Citations: `mcp_agent_pty_server.py`; `rust/crates/als-server/src/ipc.rs`; `rust/crates/als-server/src/sidebar_ipc.rs`; `DEVELOPER_MESSAGE_TEMPLATE.md`; `/data/data/com.termux/files/home/mrselect6/docs/planning/FILE_EDITOR_CM6_SIDEBAR_DRAFT_DISCOVERY_AND_AGENT_EDIT_REVIEW_PLAN.md`

### KB deterministic Markdown mutation spacing

- Fact: KB mutation helpers in `mcp_agent_pty_server.py` now default to deterministic Markdown spacing for `kb_write` and `kb_update`: auto mode strips outer blank input, preserves exactly one blank line after ATX headings, preserves a blank body after section headings even for empty replacements, and keeps `spacing="preserve"` for exact raw replacement/insertion.
- Why it matters: KB reads/searches can stay Markdown-oriented, but mutation defaults must not silently collapse heading/body boundaries or produce style-broken Markdown. Future KB writer changes should keep deterministic auto formatting separate from explicit raw/preserve mode.
- Citations: `mcp_agent_pty_server.py`; `KB_MEMORY_MCP.md`; `kb_tool_issues.md`

### ALS-RS stateful sidebar app windows

- Fact: ALS-RS is TE2 stateful-sidebar-capable through the TE2 wrapper `sidebar_state` manifest for `kind: "conversation"`, with `te2_host_id` as the only gate for stateful behavior. Normal `/app/als-rs` and ordinary `?conversation_id=...` deep links remain non-stateful unless TE2 supplies slot identity.
- Fact: Stateful ALS-RS window hydration is client-local: the frontend reads `conversation_id` / `view` query params, calls explicit `conversation.get`, replays the transcript, and does not call `conversation.select` as part of cold slot hydration. User-driven conversation switching inside that slot can publish new slot state after the conversation is loaded.
- Fact: ALS-RS publishes sidebar window checkpoints through generic `/rpc/ui` method `app.windowState.publish`; Rust validates `host_id` and `conversation_id`, builds the restore URL under `/app/als-rs`, and calls TE2 `/sidebar_ipc` method `sidebar.window.state.update`. This checkpoint path does not post TE2 app/backend readiness.
- Fact: ALS-RS backend registers its persistent TE2 `/sidebar_ipc` client as app `als-rs` and consumes `sidebar.window.focused` notifications. Rust keeps an in-memory last-focused-window ledger by deriving `conversation_id` from `query_state` / `queryState` first, then `restore_url` / `restoreUrl`, then `url`; sidebar mentions target explicit `conversation_id` first, the focused-window conversation second, and the persisted active conversation last. Focus-derived mention inserts are broadcast as conversation-scoped live insert events so the matching stateful iframe can handle cursor insertion.
- Why it matters: TE2 may keep multiple ALS-RS sidebar slots open for different conversations. `app_id` identifies the app lane, `host_id` identifies one durable slot, and TE2 stores ALS conversation query state as opaque app-owned state without scraping iframe URLs or letting the frontend talk directly to `/sidebar_ipc`. Backend readiness remains app-lifecycle state from Rust server startup, separate from stateful window checkpoints, while focus notifications provide only the transient default-target hint ALS needs for mention routing.
- Citations: `worktrees/notes/ALS_RS_STATEFUL_SIDEBAR_APP_WINDOWS_PLAN.md`; `rust/crates/als-server/src/static/codex_agent.ts`; `rust/crates/als-server/src/state.rs`; `rust/crates/als-server/src/sidebar_ipc.rs`; `rust/crates/als-server/src/ui_rpc.rs`; `rust/crates/als-server/src/te2_readiness.rs`; `/data/data/com.termux/files/home/mrselect6/app/apps/als_rs/manifest.json`; `/data/data/com.termux/files/home/mrselect6/app/apps/als_rs/main.js`; `/data/data/com.termux/files/home/mrselect6/docs/apps/code_cm6/STATEFUL_SIDEBAR_APPS.md`.
