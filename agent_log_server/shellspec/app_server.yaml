version: "1"
shells:
  app_server:
    backend: pipe
    cwd: ${CWD}
    subgroups: ["codex_agent", "app_server", "codex"]
    command:
      - codex
      - app-server
    labels:
      app: "codex-app-server"
